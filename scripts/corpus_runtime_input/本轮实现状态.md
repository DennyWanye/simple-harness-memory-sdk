# 第一层编译器实现与验收状态

最后更新：2026-09-06。独立worktree基于 Memory main `1a53e587d820df486ccdc2eaf92a5a29866a0526`，分支 `feat/corpus-runtime-input`。本目录新增标准库编译器、隔离契约及验收脚本，并同步ARCH事实；原r4文档、旧JSONL、generator、spec、metric和阈值未修改。

编译器的15项源契约验收已覆盖并闭合。240个逻辑样例、12类各20条、C05的22条固定后续轮、C07两组显式角色历史以及C04-15的统一时钟覆盖均保留；真实CLI重复输出字节一致。gold和setup变异覆盖全部240例，followup变异覆盖22条；首轮输入不受这些内容变异影响。CLI拒绝变更后的源指纹，变异测试仅操作内存或ignored副本。

**这不是语料/模型质量PASS。** 21例混合可信输入仍阻断投影；全部240例 `ready=false / executed=false`，公共seed为null。没有调用模型、Provider、SDK或native，没有模拟真实候选/授权/历史trace，也没有改变正式质量分母。

## 已运行批次与修正

| 批次 | 结果 | 说明 |
|---|---|---|
| `corpus-runtime-input-acceptance-20260906-r1` | 15项：14通过、1失败 | test_09的变异替换首次出现的provider_input，误改了前言而非字段；编译器不应拒绝该内存内前言变更 |
| `corpus-runtime-input-acceptance-20260906-r2` | 定向3项通过 | 变异定位改为完整字段标记；复验test_09、test_11、test_14。后两项因acceptance.py进入编译器指纹而更新重复编译/CLI证据 |

未修改编译器生产逻辑来迎合测试。首轮失败原始日志保留；最后批次是3项定向复验，不冒称最后一次全量15项重跑。其余12项仍引用首轮结果。

两批均使用已核对的资源入口 `145baed3940d0a69aab1e38918efafa975e6afba`，默认共享锁、512MiB RSS上限、90秒期限，Python为3.12.14。资源parent退出码分别为1和0；均无强制停止、cleanup_error或残留成员。进程组27786、27811另经`ps`复查为0条，测试槽已通知主代理释放。RSS为快速采样值，不据此给性能结论。

## 精确命令

工作目录为 `/Users/denny/projects/simple-harness-memory-sdk-corpus-runtime-input`。下面变量只是缩短重复路径；没有覆盖默认lock-file。重放时须换新批次名，不能覆盖既有证据。

```sh
CORPUS_PYTHON=/Users/denny/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3
CORPUS_RESOURCE_RUNNER=/Users/denny/projects/simple_harness-test-resource-cleanup/scripts/run_resource_bounded.py
CORPUS_EVIDENCE_ROOT=/Users/denny/projects/simple-harness-memory-sdk-corpus-runtime-input/.local-test-evidence

"$CORPUS_PYTHON" "$CORPUS_RESOURCE_RUNNER" \
  --evidence-dir "$CORPUS_EVIDENCE_ROOT/corpus-runtime-input-acceptance-20260906-r1/resource" \
  --rss-mib 512 --seconds 90 -- "$CORPUS_PYTHON" -B -m scripts.corpus_runtime_input.acceptance \
  --artifacts "$CORPUS_EVIDENCE_ROOT/corpus-runtime-input-acceptance-20260906-r1/artifacts"

"$CORPUS_PYTHON" "$CORPUS_RESOURCE_RUNNER" \
  --evidence-dir "$CORPUS_EVIDENCE_ROOT/corpus-runtime-input-acceptance-20260906-r2/resource" \
  --rss-mib 512 --seconds 90 -- "$CORPUS_PYTHON" -B -m scripts.corpus_runtime_input.acceptance \
  --artifacts "$CORPUS_EVIDENCE_ROOT/corpus-runtime-input-acceptance-20260906-r2/artifacts" \
  --case test_09_unknown_duplicate_or_missing_fields_rejected \
  --case test_11_repeat_compilation_and_blocked_setup_outputs \
  --case test_14_cli_compiles_twice_identically_and_preserves_sources
```

## 本地证据索引

下列路径均相对本worktree的 `.local-test-evidence/`；raw和机器数据已核对ignored，不提交Git。

| 文件 | SHA-256 |
|---|---|
| `corpus-runtime-input-acceptance-20260906-r1/resource/command.log` | `fb691656cbacee5c561db5ce98c25c34715230cb3da8d18da5a614d2fae01973` |
| `corpus-runtime-input-acceptance-20260906-r1/resource/resource.json` | `c2c23761ccad74109517dc82e74d295d1e768487c52e9ef862b243930ae25cde` |
| `corpus-runtime-input-acceptance-20260906-r2/resource/command.log` | `f45f6e4bcaf647ffc6d4dd7734952c0ba7d2375e0b79f12ec29f390061501325` |
| `corpus-runtime-input-acceptance-20260906-r2/resource/resource.json` | `e279cb08a504f0545e775bcde12000ac3a7e537a91bcaff72166dc174c7d3080` |
| `corpus-runtime-input-acceptance-20260906-r2/artifacts/cli-one/manifest.json` | `ac79f8f39d337affd408e359be4215246ef6938d0a71f1201e348e2e1005b106` |

末批 `artifacts/cli-one` 和 `cli-two` 的8个输出文件逐文件字节一致；manifest记录所有成员与输出指纹。r4原13成员批次指纹仍为 `2a69f5712991f665eafed67d5457f7d6b8484885ab186927b57f5a7014c576d1`，原总索引另锁定且未改。

## 剩余接线

公共setup、统一真实政策和三端时钟适配全部待实现；C12全部20例与C05-07共21例需要明确可信上下文/用户消息分字段绑定，不能猜enum/授权；C07两例需要真实历史入口；C05需要真实事件调度及C05-18 revision操作。C12-20保持单一正式A存在世界。详细接口及各字段可见性见[接口契约](接口契约.md)。当前分支仅具备经验证的源编译能力，等待主代理代码复核，未合并到main或发布。
