# 2026-09-06 用户执行指令更新

最新依据为当前会话中的用户明确指令：

> 不要使用plan-test技能，但是完成其原始文档中剩余的任务

> 直接给md文档给我就好。另外太多了，我不想要审查，你让一个子代理审查，你review一下就好。

## 当前执行方式

- 停止使用 plan-test、plan-task、plan-bs 技能及其编排；不再将这些技能的确认、机器账本或 finalize 流程作为继续开发的前置。
- 原 plan、acceptance 中的产品目标和剩余功能继续实施；保留真实的公共 SDK、故障、UI、Provider、质量与性能验证。历史失败和历史记录不重写，不将流程取消记为功能通过。
- 审阅材料仅交付 Markdown，不再制作交互 HTML。之前生成的未跟踪 HTML 副本已移除，原始语料和 Markdown 副本保留。
- 240 条语料的复审责任改为一个子代理逐条审查，主代理复核。此项用户决策替代旧文档中“必须用户安排真人复审”的执行前置；不再要求用户逐条审阅或再次确认。
- 审查来源必须如实记为子代理审查及主代理复核，不能冒称人工标注。若发现标签错误或覆盖不足，修订候选集并保留旧版本；主代理复核完成后固定新评估版本。审查本身不等于真实模型质量指标达标，仍须执行原定真实评估和阈值验证。

## 当前正在处理

1. 主代理：最新原生版本的遗忘确认、图谱恢复、重启持久化；同步事实文档。
2. 子代理：240 条语料逐条审查及中文问题报告，主代理接续复核。
3. Harness 审计负责人：Host 安装版 0.7.3 审计终态身份绑定、反例和恢复测试。
4. Host UI 负责人：目的和权限绑定的受控审计查看入口。

后续继续短期索引调度/召回接线、任务状态与预算、程序/提醒、图谱关系与可读性、401 条确定性测试及真实模型质量性能验证。此记录不宣称上述工作已完成。
