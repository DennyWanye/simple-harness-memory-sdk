# Handoff — Human Memory 数字孪生 Program（2026-09-07）

> 交接给新 session。**先读本文件，再读 §7 列出的权威文档。**
> 本文件由 2026-09-07 会话生成，汇总了：①当前完成度统计；②本会话刚完成的「三仓代码全部推送到远程」动作；③给接手者的下一步。

---

## 0. 本会话刚做了什么（代码已全部上远程）

用户要求「把所有有用代码提交到远程 main 分支，SDK 的也要」。已完成：

| 仓 | 远程 URL（均私有 GitHub，DennyWanye 名下） | main 推送到 | main 本次推送提交数 |
|---|---|---|---|
| **Host** `simple_harness` | `git@ssh.github.com:443/DennyWanye/simple_harness.git` | `c183fe70` | +7 |
| **Harness SDK** `simple-harness-sdk` | `git@ssh.github.com:443/DennyWanye/simple-harness-sdk.git` | `2b84284` | +1 |
| **Memory SDK** `simple-harness-memory-sdk` | `git@ssh.github.com:443/DennyWanye/simple-harness-memory-sdk.git` | `72e5570` | +39 |

- **所有本地 feature 分支已全部 `push --all` 到 origin**（三仓合计约 120 个分支，含全部 `feat/*`、`feature/*`、`fix/*`、`codex/*`、`wt/*`），无一遗漏。
- **标签已 `push --tags`**；harness-sdk 的 `v0.1.0` 报「already exists」表示远程已有，无丢失。
- 推送验证：三仓 `origin/main..main` 均为 0，未推送分支均为 0。

**刻意未提交的两处本地文件（非「有用代码」，勿动）：**
- `simple_harness/.plan-test/active-run.json`（已修改，plan-test 运行状态）
- `simple_harness/plans/taskSys/`（未跟踪，三个中文架构参考文档，属于另一条工作流）

> ⚠️ 语义提醒：这是「把已有的有用代码推到远程 main + 全部保留到远程分支」，**没有把 120 个 feature 分支合并进 main**。合并是危险动作，原因见 §5；接手者若要合并，须自行判断并逐个处理。

---

## 1. 一句话结论

**整个 program 没有完成，8 条 MUST AC 没有任何一条走到 program 级终验。** 代码层面（Memory SDK 的 S3、以及 S1/S2/S4/S5a）基本做完；真正没做完的是「HM-AC-8 质量门 + S6 UI 收口 + S5c 调度 + 完整原生 E2E」这一整段收尾。

---

## 2. 按 7 个切片统计（对比原始 plan）

| 切片 | 状态 | 说明 |
|---|---|---|
| **V0**（验收权威 + 5 项 spike） | ✅ 完成 | 验收权威、冲突测试迁移、5 项真代码 spike |
| **S1**（公共协议 + 持久化安全边界 + 同 Run 屏障） | ✅ 完成 | 公共协议、持久化安全边界、同 Run 控制屏障 |
| **S2**（永久证据 + 审计 + suppression + durable outbox） | ✅ 完成 | fresh v7 evidence、suppression、durable analysis、cognitive mutation authority |
| **S3**（四类记忆 + 类型化召回 + 图谱） | ⚠️ SDK 侧完成，质量门未过 | Task 1–7 代码全部完成；但 **240 语料真实模型质量 = 0、401-cell 矩阵未完整跑** |
| **S4**（唯一主对话 + TaskScope + 多根绑定） | ✅ 完成 | 有 SHIPPABLE 收据 `r2-p1-closure/gate-receipt.json` |
| **S5**（动态 Context + 路由/召回/语义收口） | ⚠️ 差收尾 | S5a 有 SHIPPABLE 收据 `r3-s5a`；**S5b 机器门未 finalize**（工作已越过）；**S5c 未接通**（scheduler/presentation/ack） |
| **S6**（单对话 UI + 图谱 + program 终验） | 🔶 进行中，未完成 | 主对话 UI、Cytoscape 图谱、遗忘 ACK、审计入口已开工并有局部 native 通过，完整 native 未过 |

> `HANDOFF-2026-09-05.md` 里「S6 9 个任务一个没动」是 09-05 旧说法，已过期。S6 已动了不少。

---

## 3. 按 8 条 MUST AC 统计

| AC | 当前状态 |
|---|---|
| **HM-AC-1** 单一主对话 + 永久证据 + 逻辑遗忘 | S2/S4 底座完成；S6 UI cutover 进行中；遗忘 native 局部 PASS |
| **HM-AC-2** 五类认知系统 + 证据化混合提取 | SDK 侧（S3）完成；真实质量门未过 |
| **HM-AC-3** TaskScope + 单 Run + 多根绑定 | S1/S4/S5 完成；完整 native 未过 |
| **HM-AC-4** 每轮判断 + 类型化召回 | typed recall SDK 完成；401 矩阵 / 240 质量未过 |
| **HM-AC-5** Procedure / Prospective | S3 Task 3 SDK 完成；S5c scheduler/presentation 未接通，Procedure adoption/applicability 仍在做 |
| **HM-AC-6** 动态 Context + 工作记忆 + 数字孪生 | SDK graph 完成；Cytoscape viewport native 通过；关系生成 / 密集标签可读性仍在做 |
| **HM-AC-7** 全链路审计 | S3 Task 7 audit SDK 完成；Host 逐操作审计未全覆盖 |
| **HM-AC-8** 跨仓初始化 + 故障 + 质量门 | **最大缺口**（见 §4） |

---

## 4. 剩余关键缺口（接手主线，按影响面排序）

1. **HM-AC-8 质量门（program 完成的最大拦路虎）**
   - **401-cell 冻结矩阵**：历史去重后 **224 有 PASS / 177 无 PASS**，没拼成过一次完整候选 run；391 public + 10 source 分批盘点做了，219 个 BLOCKED（122 fixture/setup、61 oracle、36 executor）。
   - **240 条语料真实质量**：AI 子代理审查 + 主代理复核已完成（语义设计基准），但**真实模型评测 0 次运行**。审查 ≠ 质量达标：required-type recall ≥90%、隐私禁止项 100%、额外类型 ≤15% 都没测。
   - **p95 ≤500ms / hard deadline 2s** 本地检索基准没跑。

2. **完整原生 E2E 没过**：遗忘确认、图谱 viewport 两段 native 通过；r8 完整 native 卡在两处真故障——旧任务后置 Provider 清理 `KeyError`（Host 最小修复 f0f72650 已绿待合入）、第 11 完整组后短期投影 `MemoryCorruptionError`。

3. **S5c（前瞻提醒）**：唯一 scheduler、signal 派生来源、occurrence/presentation/ack/settle、披露过滤均未接通；默认主 schema 仍 49，隔离叶 50 未激活完整 scheduler。

4. **S6 收口**：主对话 cutover、图谱、审计入口、遗忘 ACK 各有 leaf/局部 native 证据，未拼成完整 program 验收；密集标签可读性、关系生成真实性（r8 图谱 0 edges）待办。

5. **Procedure/Prospective 完整产品链**、**多消息 producer**、**Service 持久审计**仍未闭合。

6. **三仓 push/tag/发布**：本次已把代码推上远程 main + 全部分支；但**正式 release tag 仍未打**，是否发布仍是用户决策。

---

## 5. 分支拓扑（接手者必读，避免乱合并）

三仓都是「main + 大量 feature worktree」模式。**main 是唯一权威收敛点**，feature 分支是进行中/历史工作，**不要盲目把全部 feature 分支合并进 main**。

**main 上已收敛的内容**（即「已完成」部分）：V0/S1/S2/S4/S5a，以及 Memory SDK 的 S3 Task 1–7 代码。

**活跃 feature 分支（继续往下做的起点，按需 checkout）：**

- Host `simple_harness`：
  - `feat/human-memory-primary-candidate` —— 主集成候选，最近仍在提交。
  - `feat/corpus-*` 一大片（c01-scoring、c03-source、c04-time-setup、c05/c06/c07/c08、inference-drain、complete-dispatch 等）—— **HM-AC-8 的 401/240 质量门**，当前最热主线。
  - `feat/prospective-*`（pending-primary-admission、ack-user-notice、runtime-lane、m617-integration）—— S5c 前瞻提醒。
  - `feat/procedure-*`（adoption、draft-classification、scope-observation）—— S3 程序记忆。
  - `feat/context-route-nullable`、`feat/manual-workspace-binding-ui`、`feat/terminal-audit-identity`、`feat/audit-access`。
- Memory SDK `simple-harness-memory-sdk`（大量后继修正尚未并回 main）：
  - `feat/human-memory-procedure-current-input-successor`（+102 commits）、`feat/procedure-observation-prepare`、`feat/current-input-visibility`、`feat/analysis-retry-protocol`、`feat/prospective-signal-source`、`feat/prospective-outbox-source`、`feat/short-empty-assistant`、`feat/disclosure-audience-binding`。
- Harness SDK `simple-harness-sdk`：`feat/nullable-tool-schema`、`feat/recall-use-reservation`、`feat/run-operation-audit`、`feat/mandatory-context-action-repair`、`feat/decision-terminal-recovery-077` 等。

**冻结候选（历史，明确不要合并/修改）：** `feature/human-memory-066-candidate`、`feature/human-memory-067-candidate`、`feat/human-memory-069-compatibility-plan`、`feat/human-memory-069-existing-data-selected-sources`、`feat/human-memory-source-only-admission-068`、`feat/human-memory-cytoscape-ui`（迁移前 WIP）。原文档多处「冻结 0610/0611/069 不动、保留所有旧红」。

完整分支清单：`git branch -r` 逐个看，本文件只列关键几个。

---

## 6. 接手后的建议次序

1. 拉最新：三个仓各自 `git fetch --all && git checkout main`（main 已在远程）。
2. 读权威文档（§7）。
3. 先收 **HM-AC-8 质量门**：把 `feat/corpus-*` 分支的 401 矩阵拼成完整候选 run，跑 240 条真实模型评测（阈值见 `acceptance.md` HM-AC-8）。
4. 修 r8 两处原生真故障（Provider 后置清理 KeyError、短期投影 MemoryCorruptionError）。
5. 接 S5c scheduler/presentation/ack。
6. 收 S6（主对话 cutover、图谱可读性、审计覆盖、完整原生 E2E）。
7. 三仓 release tag（仅当用户授权）。

---

## 7. 权威文档（按优先级读）

1. **[RESUME-2026-09-05.md](RESUME-2026-09-05.md)** —— 最新接续记录，顶部 2026-09-06 各段是当前状态。
2. **[EXECUTION-UPDATE-2026-09-06.md](EXECUTION-UPDATE-2026-09-06.md)** —— 用户 09-06 的执行方式变更（停用 plan-test、Markdown-only、子代理审语料）。
3. **[EXECUTION-REVISION-2026-09-05.md](EXECUTION-REVISION-2026-09-05.md)** —— 09-05 用户批准的按产品闭环推进的执行顺序。
4. **[plan.md](plan.md)** + **[acceptance.md](acceptance.md)** —— 原始 program 范围、8 条 AC、12 个场景、401 矩阵、240 语料阈值。
5. `ARCHITECTURE/PROJECT_STATUS.md`（Memory SDK 仓）—— 官方记录的模块完成状态。
6. `slices/` 目录 —— V0/S1–S6 的 Task 清单。

---

## 8. 执行方式约束（用户 09-06 明确指令，接手必须遵守）

- **停用 plan-test / plan-task / plan-bs 技能及其编排**，不再把它们的机器账本/finalize 作为开发前置。
- 审阅材料**只交付 Markdown**，不做交互 HTML。
- **240 条语料**由一个子代理逐条审查、主代理复核；**不要求用户逐条人工复审**；审查来源必须如实记为「子代理审查 + 主代理复核」，不得冒称人工标注。
- 原 plan/acceptance 的产品目标和剩余功能继续实施；历史失败和历史记录不重写。
- 原始测试证据只落 `gitignored` 的 `.local-test-evidence/`，不入库。

---

## 9. 已知边界 / 风险（沿用此前交接）

- 完整质量结论不能提前标 PASS；AI 审查不等于真实模型质量达标。
- 主 checkout（用户正在用的 `simple_harness`）的生产运行时仍是 Host `c183fe70` + Harness 0.7.2 + Memory 0.6.3 + Service 0.3.12，**未被本次推送动作切换**。
- 凭据只在进程内与 `llm_runtime.json`，不进仓库/日志/证据/URL。
- 端口 8100/5173/5199 串行；重型测试串行，进程组/RSS/时间限额看护。
