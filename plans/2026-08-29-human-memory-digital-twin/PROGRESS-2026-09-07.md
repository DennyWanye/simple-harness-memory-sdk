# 进度表（对照原始 plan.md / acceptance.md）— 2026-09-07 晚

> 依据：`plan.md` 七个 release unit、各 `slices/*.md` 的 Task 清单、`acceptance.md` 8 条 MUST AC。状态口径：✅ 完成且有真实验证；🟡 代码完成但验收未闭合；🔶 部分完成；❌ 未做；⏸ 用户延期。"真实验证"指真实 Provider/原生 UI/公开 wheel 组合下的证据，单测不算。

## 一、七个 release unit

| 切片 | Task 数 | 完成 | 状态 | 未闭合项 |
|---|---|---|---|---|
| V0 验收权威 + 5 项 spike | 7 | 7 | ✅ | — |
| S1 公共协议 + 持久化安全边界 + 同 Run 屏障 | 7 | 7 | ✅ | — |
| S2 永久证据 + 审计 + suppression + durable outbox | 6 | 6 | ✅ | 遗忘口径按 09-07 用户决定改为"只针对记忆"（0.6.21/0.6.22 已实现并原生验证） |
| S3 四类记忆 + 类型化召回 + Procedure/Prospective + 图投影 | 7 | 7 代码 | 🟡 | Task 5 类型化召回：中文词法门已修（0.6.20），认知记忆**无向量通道**（0.6.23 已实现、待复核落地）；Task 3 Procedure 真实使用链原生 FAIL；Task 7 质量门未过 |
| S4 唯一主对话 + TaskScope Archive + 多根绑定 + 单 FIFO | 8 | 8 | ✅ | Task 6 任务搜索中文匹配 09-07 修复（语料 C05-04/09） |
| S5 动态 Context + 五路路由 + 召回观察 + 语义收口 + 分析 outbox + 生产组合 | 8 | 8 代码 | 🟡 | Task 4 ContextSnapshot 20+ 轮长对话审计（HM-TO-A6）未跑；S5c 提醒：时间触发 ✅、卡片顺序口径 ✅、事件触发 ⏸（F01）、递归提醒 backlog |
| S6 单对话 UI + TaskScope 审查 + 知识图谱 + program 验收 | 8 | 3 | 🔶 | 见下表 |

### S6 逐项

| S6 Task | 状态 | 说明 |
|---|---|---|
| 1 唯一主对话 UI cutover | ✅ | 原生 r1–r7 在 main 真实通过（写入/召回/遗忘/重启/提醒） |
| 2 TaskScope inspect/resume UI | ❌ | 后端读接口齐全（view/evidence_groups/evidence_page/search/open_exact），前端无独立任务审查界面，无验收记录。**09-07 核对新发现的缺口** |
| 3 Memory knowledge graph | 🟡 | Cytoscape 画布/筛选/遗忘同步 ✅；v6 关系抽取 + 端到端边测试 ✅；密集标签 ✅（独审 ACCEPT）；真实模型关系边原生验证待 r8 |
| 4 受控审计与普通面隔离 | 🟡 | 审计入口/分页/ACK 已合入并独审；Host 全操作覆盖未验（AC-7） |
| 5 执行 V0 sealed testcase 与 gate manifest | 🔶 | 401 矩阵 runner 已在 main 并 pin；09-07 首次扫描发现 391 cell 缺执行器接线、10 源 cell 未配置，尚无有效结果 |
| 6 自动化与性能/故障/质量门 | 🔶 | 240 语料：跑道支持 181/240；run-01 69 例跑完并复核（PASS 54 / FAIL 9 / 未评分 6）；C01 召回 73.7%（<90%）、多提类型 52.6%（>15%）、隐私 100%；性能/延迟基准未做 |
| 7 required 真人桌面 E2E | 🔶 | 三条原生旅程通过；Procedure 旅程 FAIL；两轮长对话/重启旅程未做 |
| 8 三仓架构/发布文档与 machine final gate | 🔶 | ARCHITECTURE 随每次交付更新；final gate 与 release tag 未做（tag 需用户授权） |

## 二、8 条 MUST AC

| AC | 内容 | 状态 | 缺口 |
|---|---|---|---|
| HM-AC-1 | 单一主对话、永久证据、逻辑遗忘 | ✅ | — |
| HM-AC-2 | 工作记忆 + 四类长期记忆、混合提取 | 🟡 | 中文召回向量通道（0.6.23）；提取质量随 AC-8 一起验 |
| HM-AC-3 | 永久 TaskScope、单 Run、多根绑定 | 🟡 | 后端 ✅（TC-HM-09/10/11 真实 PASS）；S6 Task 2 任务审查 UI 未做 |
| HM-AC-4 | 每轮判断、类型化召回、no-recall | 🟡 | C07 零召回 19/19 ✅；C01 精确召回未达标；401 矩阵未有效扫描 |
| HM-AC-5 | Procedure / Prospective | 🔶 | Prospective 时间触发 ✅；Procedure 真实使用链 FAIL；事件触发 ⏸ |
| HM-AC-6 | 动态 Context、工作记忆、展示型图谱 | 🟡 | 装配器/分页 ✅、r13 审计 45/45 ✅；长对话 ContextSnapshot 审计未跑；图谱 r8 待验 |
| HM-AC-7 | 全链路审计 | 🟡 | 普通对话审计与 driver 覆盖 ✅；全操作覆盖未验 |
| HM-AC-8 | 跨仓初始化、故障矩阵、质量/延迟/Token、真 UI/provider | ❌ | 质量门两项未达标；401 未有效扫描；延迟/Token 基准未做 |

## 三、汇总

- 切片：V0/S1/S2/S4 完成；S3/S5 代码完成、验收未闭合；S6 3/8。
- AC：1 条通过，5 条黄，1 条橙，1 条红（AC-8）。
- 粗估：功能实现约 85%，真实验收约 60%。
- 当前阻塞质量门的唯一大项：认知记忆向量通道（0.6.23，已实现待落地）。其次：Procedure 产品链、S6 Task 2、401 执行器接线、59 例语料适配。

## 四、下一步（按用户 09-07 顺序：质量门 → r8 → S5c/S6）

1. 复核 0.6.23 提交 → 构建 wheel → Host 重 pin（含向量恒请求、worker 重建、语料跑道显式重建）→ run-02 重跑受影响 11 例。
2. 401 矩阵：接上 cell 执行器与源执行器后重扫。
3. 重建原生包做 r8（关系边、密集标签、向量召回）。
4. S6 Task 2 任务审查 UI；Procedure 真实使用链；剩余 59 例语料适配；长对话 ContextSnapshot 审计；性能基准。
5. Followups：F01（延期）、F02、F03、F04；release tag（需授权）。

## 五、深夜追加（2026-09-07 晚 20:00–23:30，用户确认任务来源后按第四节执行）

| 项 | 结果 |
|---|---|
| Memory 0.6.23 | 认知记忆向量通道落地（方案 A）；Host 重 pin、语料跑道显式重建世代 |
| 语料 run-02（11 例重跑） | **11/11 PASS**；C01 required 召回 7/7（run-01 2/7）；路由循环消失（11→1、6→1）；`cognitive_vector_unavailable` 消失；多提类型率 71% 仍超门槛 |
| 401 矩阵 | 首扫 44 个 eligibility FAIL 根因为验证适配器未配对 `intended_audience`（非 SDK 回归）；修后正式扫描 **PASS 227 / FAIL 0 / BLOCKED 174**（0.6.23 与 0.6.24 pin 各一次，相同） |
| 原生 r8（0.6.23） | 真实模型 v6 提案 → 图谱「2 条记忆 · 1 条关系」applies_to 边 ✅；发现 relation head 使认知向量世代每 tick 抛 MemoryCorruptionError |
| Memory 0.6.24 | 世代跳过 relation head，失败落 failed 行；Host 重 pin，401 重 pin（rev 12 / layers 10）扫描不变 |
| 原生 r9（0.6.24） | 零告警；12 轮填充后零词面重叠提问触发模型 context_route（full_text+vector），long_term_typed 命中 `backup_directory_device=外接硬盘`（查询词项对 payload 0 命中 → 向量通道），终答正确 |
| 任务搜索 | 中文二字组合 + 单字兜底（C05-04/09 由 0 候选变为命中并只读 resume） |
| run-01b/01c | 剩余 112 例支持集在 0.6.24 上后台续跑（run-01b 4 例后为 pin 让位停止，run-01c 108 例进行中） |

状态更新：S3 Task 5 / HM-AC-3、HM-AC-4 由 🟡 升为 ✅（中文同义召回原生与语料均通过；AC-4 的 401 矩阵 0 FAIL，174 BLOCKED 属执行器/oracle 待补）；HM-AC-6 图谱关系真实性 ✅（密集标签 >12 节点真实截图仍未覆盖）；HM-AC-8 仍 ❌（多提类型率、剩余语料、性能基准）。S6 Task 2 任务审查 UI、Procedure 使用链未动。

## 六、深夜追加 2（2026-09-07 23:30 起）

| 项 | 结果 |
|---|---|
| S6 Task 2 任务审查 UI | 前端面板（搜索候选→精确打开→按需页入，只读）+ 后端 `task_scope.list` / `open_exact.binding_summary` / 拒绝客户端 `live_probe`；独审 ACCEPT-WITH-FIXES 的 F-1 已修；原生真实点击未做 → 🟡 |
| Procedure 使用链 | 根因：发现面只看 draft/eligible + 子串匹配，已采用流程（ACTIVE+UNBOUND）不可见；Memory 0.6.25 修复 + Host 提示词；原生 r10/r11 步 1–4 通过（真实 `procedure_discover` 非空、区分草稿与执行）；步 5 先后被 `after` 必填（已修）、参数拒绝致 history 不可核验（已修，语料 C05-07 同根因）、**provider 传输超时后 Run 停摆（F06，未修）** 阻塞 → 仍 🔶 |
| 语料 | run-01b/c/d 共 19 例 PENDING 待复核；run-01e（剩余 92 例）在 0.6.25 上后台跑 |
| 401 矩阵 | 0.6.25 pin 扫描 PASS 227 / FAIL 0 / BLOCKED 174（不变） |

新增待办：F06（provider 超时后 Run 恢复）、r12 Procedure 链重跑、S6 Task 2 原生点击验证、run-01b–e 复核汇总。

## 七、2026-09-08 凌晨追加（用户睡眠期间，无 UI 工作）

| 项 | 结果 |
|---|---|
| Memory 0.6.26 | prospective 触发条件渲染进向量/词面文本（C04 零召回根因），世代按文本格式版本重建；Host 重 pin，401 重 pin（rev 14 / layers 12）扫描 PASS 227 / FAIL 0 / BLOCKED 174 不变 |
| Host 修复 | F06 provider 超时后 Run 恢复（retry-once + 在途闸门，独审 ACCEPT-WITH-FIXES 已修）；授权运行时时钟接缝（C04-15 grant 未激活先过期）；跑道：prospective 注册 tick、C08 标量 setup、C06 gold 口径改 `required_procedure_access`、C05-12 移出 |
| 240 语料累计 | 181 例执行：**PASS 153 / FAIL 24 / 未评分 4**（C01/C07/C08/C11 全绿；C04 16/20；C06 4/19；C09 15/19）；召回率≈96% ✅、隐私 100% ✅、多提类型率 >15% ❌。记录 Host `plans/2026-09-07-corpus-c01-local/CORPUS-CUMULATIVE-2026-09-08.md` |
| 原生 | r11 步 5 被 F06 阻塞；r12 因锁屏下 GUI 脚本不可用（System Events 枚举不到窗口）未能执行，需解锁后跑；HM-TO-A6 方案与驱动脚本已备（`plans/2026-09-08-hm-to-a6/`） |
| 进行中 | F07（page_in 失败致 Run 不可核验）修复；C06 context_route 无 procedure 候选时的显式提示（P1）与 prospective `trigger_local`（P2） |

用户 09-08 决定：F01 事件来源为**本地**发布动作（待出裁决备忘）；剩余测试顺序 HM-TO-A6 → Manual 模式旅程 → 两轮完整流程 → 性能基准 → 语料剩余适配。

## 八、2026-09-08 上午追加（用户醒后，白天 UI 时段）

| 项 | 结果 |
|---|---|
| 原生 r14（DeepSeek，M0.6.26） | 步 1–4 ✅；步 5 绑定成功但模型未逐字复用绑定参数 → 按契约拒绝 → 重绑被拒 → DeepSeek 连发空参数 → `react_max_turns_exceeded`。契约正确，可执行性不足。Host 修复已合入 main（`bcd3bb15`）：回显 `bound_steps` + `next_action`、可操作拒绝文案；裁决：步骤全部成功后同 Run 允许普通工具调用（核对文件）、预派发拒绝不再使整组终态不可表示。待 r15 真实验证 |
| HM-TO-A6 | 两次尝试中止（luna 首轮超时；DeepSeek 上三缺陷）。已修/在修：① 记忆分析通道解析失败根因 = DeepSeek 工具参数多一个 `}`（31%），Host 保守修复合入（`840ad2cc`）；② 第 5 轮终态观察 74 KB 超过 SDK 64 KiB 内联上限，SDK 读取拒绝使整个主对话不可读、前台驱动停摆、短索引重放风暴（修复定稿中）；③ DeepSeek 100 万窗口使裁剪永不发生 → 启动前写 `model_overrides.toml` 限 32000。新增 `a6_verify.py` 核对脚本、驱动脚本发送确认与终态判定、`preflight_native.sh` 预检 |
| F06 | luna 传输超时 → `reconcile.unknown_settled` → 重试一次，Run 未停摆：真实环境首次验证通过 |

状态：HM-AC-5 仍 🔶（待 r15）；HM-AC-6 待 A6 正式跑；新增 Host 缺陷两项（主对话可见性停摆、分析通道解析）均在本日修复。

## 九、2026-09-08 0.6.27（世代重建嵌入移出写锁；召回取锁受 deadline 约束）

| 项 | 结果 |
|---|---|
| 缺陷来源 | HM-TO-A6 turn 22 现场：typed recall 连续 5 次 `DEADLINE_EXCEEDED`，`candidate_query_started=0`（一条候选未扫）；诊断 Host `plans/2026-09-08-hm-to-a6/DIAG-RECALL-TIMEOUT.md` |
| 根因（SDK） | 两处世代重建在 `_write_lock` 内 `embed_batch`（WeMM 6 chunk 45.7 s vs Host tick 超时 5 s）→ 永不激活的活锁，写锁占空比约 65%；前台 recall 两处取同一把锁且无 deadline |
| 修复 | 短时域与认知世代改三段式（锁内读清单 → 锁外嵌入 → 重取锁 manifest 乐观 CAS 后原子激活）；`_write_lock_before(deadline)` 使 admit / 向量 lane / `collected_epoch` / 短时域候选段的等锁都在预算内；向量 lane 等锁超时**退化** `cognitive_vector_deadline`（预留 200 ms 给词面 lane 与终态），admit 抛 `TypedRecallDeadlineExceeded(stage='admit_write_lock')`（仍是 `TimeoutError`/`DEADLINE_EXCEEDED`） |
| 契约 | 幂等/replay/空集/`failed` 行/`COGNITIVE_TEXT_FORMAT_VERSION` 全部不变；无 DDL 变化（7.4 checksum 不变）；根导出零增减 |
| 测试 | 新增 `tests/integration/test_generation_rebuild_lock_isolation.py` 7 项（活锁复现在 0.6.26 上以 `DEADLINE_EXCEEDED` 失败、0.6.27 通过）；全量失败集合与 main 基线逐条相同（63 项既有环境失败），1558 → 1565 passed |
| Host 待办 | `WeMMEmbedder.embed_batch` 覆写、chunk 文本长度上限、`PrimaryShortIndexWorker` 退避/断路、`deadline_ms` 1000 → 2000、`context_route_recall_timeout` 不应杀 Run |

状态：仅本地候选（分支 `m0627`），未合 main、未构建 wheel、Host 未 pin。

## 十、2026-09-08 0.6.28（召回权威 epoch 只跟踪"可能改变资格"的事件）

| 项 | 结果 |
|---|---|
| 缺陷来源 | HM-TO-A6 事故 F：工具 `05:40:46.066` 成功结算，`05:40:46.222` 用途围栏 `authorize_recall_context_use` 抛 `RECALL_AUTHORITY_STALE` → Harness 无处接住 → `run.fail`。用户库一次会话 26 个 epoch，`typed_recall_results` 18 行而 `recall_context_use_receipts` 只有 1 行。Host 备忘 `plans/2026-09-08-hm-to-a6/DECISION-RECALL-AUTHORITY-STALE.md` §7(1) |
| 根因（SDK） | S3 slice §5.4 把 epoch 定义为「任何**可能改变资格**的」事件，实现里 8 个抛点中有三条是**纯索引/投影重建**：短时程投影重建（纯新增时）、短时程向量世代激活、认知向量世代激活。它们不让任何一条记忆失去资格，却把一道罕见的语义围栏变成高频误报 |
| 修复 | 停止推进 `short_horizon_generation_changed` / `cognitive_vector_generation_changed`；`short_horizon_projection_changed` 收窄为仅当 `removed_chunk_count > 0`（= §5.4 的「Short-Horizon source 失效」）。保留 suppression / cleanup / procedure / prospective / cognitive mutation 五条资格车道 |
| 为何无需补检查 | 用途围栏在 epoch 判断之后紧接 `_validate_recall_context_use_sources_unlocked`，逐条重校验被绑定来源的 head/revision/`content_hash`/状态/有效期/privacy/attributes/type 权威/血缘抑制/disclosure（短时程另加 chunk 存在性与 `expires_at`），任一不成立以同一码拒绝；被停掉的车道碰不到这些字段。`history_visibility` 只读 `policy_hash`、不读 epoch |
| 契约 | `recall_authority_events`/`heads` 的 DDL 与行形状不变（epoch 单调 +1、`previous_epoch` 严格衔接、`initialized` 惰性建头 0→1）；两处围栏判据与抛出码不变；世代激活审计不变。无 DDL 变化（7.4 checksum 不变）；根导出零增减 |
| 测试 | 新增 `tests/integration/test_recall_authority_epoch_lanes.py` 4 项（3 项在 0.6.27 源上失败、0.6.28 通过；第 4 项两版皆过，正是"来源重校验已经兜住"的证据）；同步更新 `test_cognitive_vector_generation.py` 一处断言。全量失败集合与 main 基线逐条相同（63 项既有环境失败），1565 → 1569 passed |
| Host 待办 | 备忘 §7(2)（epoch 相等性降级为"来源重校验通过即签发 + 降级码"）与 §7(3)（Harness SDK 为用途围栏拒绝留同 Run 有界修复路径）本轮均不做；真正的资格变化仍会在工具结算后到用途授权之间抛 stale，那时抛错是正确行为，但 Harness 仍无出路 |

状态：仅本地候选（分支 `m0628`），未合 main、未构建 wheel、Host 未 pin。

## 十一、2026-09-08 0.6.29（用途围栏：epoch 前进但被绑定来源未变时签发收据）

| 项 | 结果 |
|---|---|
| 缺陷来源 | HM-TO-A6 第 4 次尝试 turn 15：0.6.28 之后仍死于同一处围栏。前台定型召回已结算，**异步分析车道**把上一轮的新记忆落库（`cognitive_memory_changed` → epoch +1）恰好落在 `authorize_recall_context_use` 之前 → `RECALL_AUTHORITY_STALE` → Host `recall_context_use_authority_stale` → 冻结的 Harness SDK 该 hop 无修复路径 → `run.fail`。证据 `simple_harness/.local-test-evidence/2026-09-08/native-a6-run4/primary-ui-*/native.log`；Host 备忘 §7(2) |
| 根因 | 0.6.28 掐掉的是索引噪声；剩下的推进车道全是**真实资格事件**，而每轮分析车道都在该轮结束后 ~10–60 s 落库，正好落在**下一轮**召回与用途授权之间。这是正常使用中的高频竞态，靠分类已消不掉，只能改判据 |
| 修复 | epoch **相等性** → **倒退性**：`policy_hash` 不等、结果期限已过、`epoch < 绑定 epoch` 三者仍硬失败；`epoch > 绑定 epoch` 交给同一把写锁、同一 `BEGIN IMMEDIATE` 事务里紧接着的 `_validate_recall_context_use_sources_unlocked` 逐来源裁定，全通过才签发收据，任一不成立仍以同一稳定码、零 payload、零收据行拒绝。`execute_typed_recall` 的工具内围栏语义一字不动 |
| 契约论证（S3 §5.4） | 该条款具名的 suppress/revoke/supersede/contest/classification 变化/Short-Horizon expiry 全是**作用在某条来源上**的事件；作用在本次绑定的来源上时逐来源重校验必然发现并抛同一码。收据只绑定 `item_bindings` 逐条列出的条目，重校验正是对这批条目做的——**被授权的集合 = 模型已经看到的集合 = 被重校验的集合**。epoch 是保守快捷判据、逐来源重校验是精确判据；保留精确、放宽保守，披露完整性只增不减。`history_visibility.py:321` 早已是这个写法 |
| 降级码零 DDL | `RecallContextUseReceiptV1` 属**冻结的 Harness SDK**（`from_json` 走 `_exact_keys`）加不了字段；两个 epoch 本就分别落在不可变的 `recall_context_use_receipts.authority_epoch` 与 `typed_recall_results.result_json`，由收据 `result_id` 唯一连接——事实由两条不可变行严格导出，冗余标记只会引入新的损坏形态。新增 `core/recall_context_use.py`（稳定码 `authority_epoch_advanced` + 有界只读视图 `RecallContextUseAuthorityNoteV1`）、只读导出 `read_recall_context_use_authority_notes`（backend + `MemoryManager`，不写行）与一行无载荷结构化日志 |
| 契约面 | 无 DDL 变化（7.4 checksum 不变）；根导出零增减（新常量与视图只经 `core.recall_context_use` 可达）；快照 `public-api-0.6.29.json` 除 `version` 外与 0.6.19 起逐字相同 |
| 测试 | 新增 `tests/integration/test_recall_context_use_fence.py` 7 项（5 项在 0.6.28 源上失败、2 项两版皆过）；无竞态路径的 `receipt_id`/`receipt_hash`/canonical `receipt_json` 与 0.6.28 **逐字节相同**（独立 0.6.28 worktree 跑同一脚本 `diff` 为空，钉死 `af05933f…c07ce`）。同步更新 `test_duplicate_source_short.py` 一处期望（它压制的是认知记忆，与该用例绑定的 short-horizon 来源无关，0.6.29 起应放行）。全量失败集合与 main 基线（`e554c20`）逐条相同（63 项既有环境失败，`diff` 为空），1569 → 1576 passed / 8 skipped |
| Host 待办 | **不需要读任何新字段**：返回类型与字段逐字不变；要统计降级，直接比对 `receipt.authority_epoch` 与手上召回结果的 `authority_epoch`。`read_recall_context_use_authority_notes` 是可选审计入口。备忘 §7(3)（Harness 为用途围栏拒绝留同 Run 有界修复）仍值得做——0.6.29 之后剩下的 stale 全是**正确**的拒绝，但 Harness 依然只会把它变成 `run.fail` |

状态：仅本地候选（分支 `m0629`），未合 main、未构建 wheel、Host 未 pin。

## 十二、2026-09-08 下午/晚（Host 侧，按事件编号）

| 事件 | 内容 | Host 提交 |
|---|---|---|
| A/B | standalone 路由下工程效果工具在 describe/activate 即披露不可激活；缺参数拒绝回显期望形状；task_scope_search 零命中引导 continue_active | `c70f568f` |
| C/D | mutate 类关闭不再受脏态门限；工具参数非 JSON 的协议错误重采样一次 | `01f9e68f` |
| E | 同 Run 内工具结果累积有界（最旧结果转可翻页引用），快照回执记录 current_tool_pages | `8f9aeee6` |
| F | 召回授权过期：工具内有界重收集；使用侧稳定码（根治 SDK 0.6.28/0.6.29） | `8bbb44b4` |
| G | 18 KB 消息静默丢弃：64 KiB 命名上限 + 稳定码 + 前端提示 | `ec13a9f8` |
| H | 明确纠正 cue-anchor 语法（不再限于两条整句模板）；分析协议 v7 `contest_semantic` | `ad7cf950` |
| I | 召回超时 Host 侧：WeMM 分组批量嵌入、短索引维护退避、召回期限 2000 ms | `7c0f5f91` |
| J | `memory_forget` 工具稳定拒绝码与文案；日志脱敏正则修正 | `4195361d` |
| K | 历史工具调用参数回放为 `{}` 的根因（100% 回放；与空参数率剂量-反应）：Run 内备忘录回填；call_id 复用失败关闭 | `5486e25b` / `26395f01` |
| M | 记忆列表按 memory_id 折叠头版本（争议时 SDK 视图含两版本）；前端逐条降级 | `077f2340` |
| O | 争议态召回披露两候选与"须确认"（此前争议短路后 0 片段）；A6-7 核对改按头版本推进 | `658c603e` |
| SDK | 0.6.27 锁外三阶段世代重建 + 带期限取锁；0.6.28 epoch 只随资格变化；0.6.29 使用侧围栏逐项复核；Host pin 0.6.28（`e721c926`），401 重扫 PASS 227 / FAIL 0 / BLOCKED 174（`3694730a`） | — |
| 语料 | C10 跑道 20/20（`f234d216`）；C12/零散案例、剩余 16 例 C10 进行中 | — |
| 基准 | `scripts/benchmark/hm_benchmark.py` + 首份报告：召回 p95 在修复后仍需实测；provider 端输入 token 超预算（估算器偏低约 40%，事件 N 修复中）；多提类型率 28.6% ❌ | `58bcce1b` |
| A6 | 尝试 4（0.6.28）：PASS 6 / FAIL 7 / INCONCLUSIVE 5；剩余根因 K/L/M/N/O 已修或在修，第 5 次待 L/N/0.6.29 pin 后 | — |
| 用户决定 | F01 延后（方案 B 下轮）；release tag 待真人验收；对话遗忘记 F10（A6 T23 改 UI 遗忘） | `fddeedb4` |

## 十二、2026-09-08 0.6.30（短时域 chunk 长度上限；世代重建只嵌入新 chunk；测试命令修复）

| 项 | 结果 |
|---|---|
| 缺陷来源 | HM-TO-A6 turn 22：单条 29 778 字符短时域 chunk 真实嵌入 23.9 s、一次世代重建 45.7 s（Host 备忘 `DECISION-RECALL-TIMEOUT-HOST-SIDE.md` §2、`DIAG-RECALL-TIMEOUT.md` §2.3）。一组一条、无上限；Host 因 `public_text_hash` 绑定无法限长；S3/S5 无长度条款。且这样的 chunk 永远放不进 `RecallBudget.max_bytes`——既拖垮维护又不能被使用 |
| 裁定 | 切段而不是截断（备忘 §2 方案表）：一条 chunk ≤ 2 048 码点，超长完整因果组按注册边界 → 段落 → 句子 → 空白 → 硬切确定性切成 ≤ 8 条内容寻址 chunk，尾部不投影并审计；每段血缘 = 整组；零 DDL——分段 chunk 在 `causal_group_id` 列存投影键 `<id>\x1f<k>/<K>`，注册拒绝含 U+001F 的 id；schema 7.5 推迟。并把世代重建改为沿用 active 世代同 `chunk_id` 的向量、只嵌入新 chunk（切段只让单条有界，全量重嵌入才是 tick 的乘数） |
| id 稳定 | 未分段组 payload 与 0.6.29 逐字相同；测试钉死在 0.6.29 源（`c025c98`）上算出的 `short:a70e0fc0…983a`/`short:2157a1a3…7d2f`；0.6.29 对超长组产出的 `short:16cf4779…3710` 在 0.6.30 打开不判损坏、重建后被 8 段替换 |
| epoch | 遗留超长行被替换 = 既有 `chunk_id` 移除 → 按 0.6.28 车道恰好推进一次 `short_horizon_projection_changed`；新超长组首次投影与向量沿用均不推进。与 `DECISION-2026-09-08-recall-authority-epoch.md` 一致，无新车道 |
| 契约 | S3 Task 4 追加 §4-补（2026-09-08）九条；S5 追加 Host 约束；决策备忘 `DECISION-2026-09-08-short-horizon-chunk-cap.md`。无 DDL 变化（7.4 checksum 不变）；根导出零增减；快照 `public-api-0.6.30.json` |
| 测试 | 新增 `tests/unit/test_short_horizon_chunk_cap.py` 8 项 + `tests/integration/test_short_horizon_chunk_cap.py` 7 项（事故原型夹具）。全量失败集合与 main 基线（`c025c98`，独立 worktree）逐条相同（63 项既有环境失败，`diff` 为空），1576 → 1591 passed / 8 skipped |
| 测试命令 | `uv run --frozen --group dev pytest -q` 此前因 `uv.lock` 钉住不存在的 `../simple-harness-sdk-memory-program` 而失败；只改 lock 中 6 处目录为 `../simple-harness-sdk`（`uv lock --check --offline` 通过、依赖版本零变化），离线 `uv sync --frozen --group dev` 与该命令可用；README 记录前提 |
| Host 待办 | 生产逻辑不得假设"一组一条"（现有 `projected_chunk_count == 1` 断言因夹具很短仍成立）；`WeMMEmbedder.embed_batch` 分组阈值可按段长重标定；维护实测常量在 0.6.30 后只对新 chunk 计成本。SDK 后续：世代构建部分进度、认知向量世代沿用向量 |

状态：仅本地候选（分支 `m0630`），未合 main、未构建 wheel、Host 未 pin。


## 十二、2026-09-08 0.6.31（一个未决 conflict group 不再短路整条 typed-recall 车道；confirmation 成员可被历史绑定）

| 项 | 结果 |
|---|---|
| 缺陷来源 | HM-TO-A6 run4 事件 O（Host 备忘 `simple_harness/plans/2026-09-08-hm-to-a6/DECISION-CONTESTED-DISCLOSURE.md` §1.4、§7 F-O-3 / F-O-1）：13 个 head 里一个 contested head（`proofreading_python_version`），任何同主题查询都被短路成 `needs_user_confirmation` + `items=()`；用修改前源码对 run4 库重放，「校对结果存到哪里」「秋分资料整理的目标是什么」均 0 item（group 成员的 qualifiers「做资料校对时」被「校对」「资料」二字组合命中） |
| 裁决 | `DECISION-2026-09-08-conflict-short-circuit.md`：S3 §5.3 只约束 contested 候选自己的载体，§5.1/§5.2 约束成员与 group 原子性，§5.6 从未把 group 排在普通候选前——契约**不要求**整次扣住；但冻结的 Harness `RecallDecisionV4`（`recall_protocol_v4.py:438-445`）一次只能带 items 或 groups 之一。于是把 group 的**准入**收窄到**槽位级相关**：词面只看 `contested_slot_text`（两名成员取值不同的公开字段 + semantic `predicate`）；向量要求成员余弦 ≥ 0.45 且不低于同类型任一普通候选；entity/task_scope/temporal 只过滤与排序。用 run4 真实词面分数否决了"同一全局序谁在前谁占载体"：依赖争议值的「校对流程用哪个 Python 版本」里 episode 8 分、group 3/2 分 |
| 修改 | 新增 `features/conflict_slot.py`；`sqlite_v5.py` `execute_typed_recall` 先收普通候选再以 `ordinary_candidates=` 调 `_collect_typed_recall_confirmation`，后者资格门一字未改、准入改为槽位级；`history_visibility._recall` 接受 confirmation 成员绑定（`result_member_hash` 核对，整组重校验，一侧不可见即整组 stale） |
| 契约面 | 无 DDL 变化（7.4 checksum 不变）；根导出零增减；Harness v4 wire 形状不变；`build_host_confirmation_execution`、预算、终态、幂等、0.6.27–0.6.29 锁/epoch 判据不动；无冲突库 decision/result/page/receipt/terminal hash 与 0.6.29 worktree `diff` 为空并钉死字面值；快照 `public-api-0.6.31.json` |
| 重放 | 修改后同库同查询：「校对结果存到哪里」`recall` 3 item（含 `proofreading_results_storage_directory`）、「秋分资料整理的目标是什么」`recall` 5 item；「校对流程用哪个 Python 版本」「这套校对流程按哪个 Python 版本执行」仍 `needs_user_confirmation`；「画画」不变 |
| 测试 | 新增 `tests/integration/test_typed_recall_conflict_short_circuit.py` 6 项；更新 `test_public_recall_rejection.py` 正控的收集器观测顺序。全量失败集合与 main 基线（`c025c98`）逐条相同（63 项既有环境失败，`diff` 为空），1576 → 1582 passed / 8 skipped |
| Host 待办 | `project_contested_confirmation` 不需要改形状；空 fragments + 无 conflict_notice 不再等于"库里没有争议"；方案 B（成员投成正经 fragments + 绑定链）SDK 侧已无阻碍；F-O-2 仍是 Host 侧独立缺陷 |

状态：仅本地候选（分支 `m0631`），未合 main、未构建 wheel、Host 未 pin；0.6.30 由另一分支并行，版本序合并时统一。

## 十三、2026-09-08 晚（Fable 并行批次，19:00 起）

| 项 | 结果 | Host 提交 |
|---|---|---|
| 语料跑道 | C10 20/20、C12 20/20、零散 12 例补齐 → **232/240 可执行**；剩余 8 例：C05-12/13/16/17 需产品决定（单主体前台、字段级抑制、分层披露、命名根），C05-18/C02-19/C03-20 跑道后续，C06-01 映射待定 | `f234d216` / `1962b932` / `52d081cd` |
| 401 矩阵 | run-11 接线 44 格：PASS 227 → **271**，FAIL 0，BLOCKED 174 → 130（执行器 20 / 无见证 24 / 定义冲突 86）；第二轮进行中 | `133b8e80` |
| 基准 | `scripts/benchmark/hm_benchmark.py` + `BENCHMARK-2026-09-08.md` | `58bcce1b` |
| 审计覆盖 | 核验器：22 类操作发射覆盖 741/741 与 969/969，无泄漏；受控面缺口 G1/G2/G6 修复中 | `fd89f5ca` |
| SDK | 0.6.30 短时视野分块上限 + uv.lock 测试命令；0.6.31 争议短路限定槽位、确认组成员可绑定；wheel 已出，Host pin 待语料批次结束 | SDK `56a66b5` / `ff8be5f` |
| 进行中 | L（关系提取）、N（token 估算校准）、F-K1（历史投影工具参数）、审计面 G1/G2/G6、C10 剩余 16 例、C12 剩余 16 例 + 新支持 9 例、401 第二轮 | — |

## 十四、2026-09-08 深夜 – 09-09 凌晨（额度重置后，子代理改用 Opus 5）

| 项 | 结果 | Host 提交 |
|---|---|---|
| 合入 | 事件 L（分析协议 v8，关系端点为既有记忆、指代字面值拒绝）、N（token 估算按 wire 口径校准，306 对请求 0 低估）、F-K1（历史投影带工具参数，v55 侧记录）、审计面 G1/G2/G6（`primary.audit.host.page`）、401 重 pin 0.6.31 + 第二轮（PASS 279 / FAIL 0 / BLOCKED 122）、语料三批复核（41 例：33 PASS / 4 FAIL / 4 NOT_SCORED；累计 234 执行 / 208 PASS / 20 FAIL / 6 NOT_SCORED；召回 98.5% ✅、隐私 0 ✅、多提类型率 24.2% ❌） | `a6c8b7c7` / `8e39f018` / `383a1c84` / `01cae06a` / `b7b5bcd0` / `3b8a9b48` |
| Memory SDK | 0.6.31 pin（`f7b14325`）：0.6.29 使用侧围栏、0.6.30 分块上限 + 测试命令、0.6.31 争议短路槽位化 | — |
| A6 尝试 5 | **PASS 11 / FAIL 1 / BLOCKED 1 / INCONCLUSIVE 5**：K/M/O 生效（空参数 0、列表可遗忘、争议期要求确认）；余项归因 L 未上线、SDK 900 s 墙钟（事件 Q）、模型分块读文件 + 路由漂移（事件 A 残留在 SDK 屏障） | `418c3c7b` |
| 新发现 | 事件 P（`:input-v1:` 守卫与 CLAIMED→RUNNING 竞态）、事件 Q（TerminationLimits 未设 max_wall_seconds；PERSONA 未枚举 direct_standalone/continue_active） | 子代理修复中 |
| 进行中 | P、Q、多提类型率策略（协议 v9）、401 86 格定义冲突独立裁决 | — |

## 十五、2026-09-09 凌晨（01:00–01:40）

| 项 | 结果 | Host 提交 |
|---|---|---|
| 合入 | 事件 P（当前输入守卫 CLAIMED→RUNNING 竞态）、事件 Q（驱动墙钟显式 600 s + PERSONA 五路由，C05-05 路由 0/8→4/4）、多提类型率（指标只取决于主模型 memory_types；schema 四条选择规则，配对回放多提 10/10→1/10）、A6-9/10 可计算判定（投影实现符合 S3 Task 6 两层规则，计划口径修正；尝试 5 复判 PASS 13） | `a359a9c9` / `26247ea8` / `d5c72465` / `72578e53` |
| 401 矩阵 | 独立裁决 86 格（run-14 PASS 355）+ 第三轮（run-15 **PASS 366 / FAIL 0 / BLOCKED 35**）；第四轮进行中；SDK 0.6.32 公共见证增量进行中 | `9877bf5e` / `3f4af60b` |
| 旅程方案 | Manual 模式旅程（15 轮）与两轮重启旅程（22 轮）的方案、驱动、核对器已就绪 | `95224260` |
| 阻塞 | **模型通道全部不可用**：DeepSeek 402 余额不足、luna 502 上游禁止、第三中转 402——待用户充值或修中转；探测器每 10 分钟自动检查 | — |
| 待处理 | 三项提示词/估算改动叠加使 8192 档分页兜底测试变红，子代理压缩措辞中 | — |

## 十五、2026-09-09（Memory 0.6.32：401 矩阵四个公共见证增量，分支 `m0632`）

| 项 | 内容 |
|---|---|
| 需求来源 | Host 封存 401 矩阵 `simple_harness/plans/2026-09-07-corpus-c01-local/TYPED-RECALL-401-RUN-08.md` §2.I-1 / §2.I-2 与 §7 后继第 4 项（精确 file:line 在同目录 `TYPED-RECALL-401-RUN-07.md` §3.1 的 D / E 两组）：8 格因公共面缺见证停在 `SDK_INCREMENT_REQUIRED` / `ORACLE_GAP` |
| 裁决 | `DECISION-2026-09-09-matrix-public-witness-increments.md`：(a) 策略版本写进 policy id（`typed-recall-eligibility/v{n}`）而非另加键——v1 payload 逐字不变；**判据（纯读）与对齐（带审计的写）拆开**，因为把对齐写进 authorize 事务会被拒绝路径的 ROLLBACK 抹掉。(c) 拒绝理由**不可从既存行重算**（`exception_fingerprint` 恰由 `reason_code` 导出），故必须存精确码——与 0.6.29「两个 epoch 由不可变行严格导出、不存冗余标记」方向相反且有依据。(d) 冻结的 `TypedRecallResultItemV1` 加不了字段、durable 侧没有逐项 lane 行（做审计入口＝新增 DDL），故挂在 SDK 自有的根导出 `TypedRecallExecution` 上，不进任何 hash 域 |
| 公共调用 | (a) `build_human_memory_v7(..., recall_policy=RecallEligibilityPolicyV1(n))` + `read_recall_policy(principal=…)`；(b) `cleanup_short_horizon(principal=…, now=None)`；(c) `read_memory_mutation_validation_notes(principal=…, plan_id=…).reason_code`；(d) `TypedRecallExecution.executed_lanes` / `.item_lane_witnesses[i].lanes` |
| 契约面 | 无 DDL 变化（7.4 checksum 不变）；根导出零增减（新符号只经 `core.recall_policy` / `core.mutation_rejections` / `core.recall` 可达）；公共错误类、冻结枚举、Harness v4 wire 形状不变；快照 `public-api-0.6.32.json` 除 `version` 外与 0.6.19 起逐字相同 |
| 字节回归 | 默认部署的 `policy_hash` / `decision_hash` / `result_hash` / `receipt_hash` 在 0.6.31 源 worktree（`ff8be5f`）与本分支各跑一次同一脚本，`diff` 为空并钉死字面值。唯一有意的字节变化是 contest 家族拒绝审计行的 `reason_code`（本增量交付物），非 contest 拒绝以反例单测钉死不变 |
| 测试 | 新增单测 17 项 + 集成 8 项；快照测试增加 0.6.32 段；`test_memory_0623_schema_cutover.py:77` 版本钉死顺延。全量失败集合与 main 基线（`fc7fa88`）逐条相同（63 项既有环境失败，`diff` 为空），1597 → 1622 passed / 8 skipped |
| Host 待办 | 401 可按备忘 §1.4 / §2 / §3.3 / §4 接线上述 8 格。RUN-08 §7 其余后继：H 组 4 格与 I-3 属纯 oracle 侧接线（不需要 SDK 增量）；分页零候选读见证 4 格需要给 `page_typed_recall_result` 的四条裸抛路径接上 `_attach_pre_candidate_rejection`，是独立一轮 |

状态：仅本地候选（分支 `m0632`），未合 main、未构建 wheel、未 push、Host 未 pin。

## 十六、2026-09-09（Memory 0.6.33：候选收集段 deadline 的取消安全修复，分支 `m0633`）

| 项 | 内容 |
|---|---|
| 缺陷来源 | Host 401 矩阵第四轮 `simple_harness/plans/2026-09-07-corpus-c01-local/TYPED-RECALL-401-RUN-10.md` §4.4 附带发现 / §10 后继第 1 条：`deadline_ms=1` 耗尽在**候选收集阶段**时抛 `sqlite3.OperationalError: cannot start a transaction within a transaction` 而不是 `TimeoutError("DEADLINE_EXCEEDED")`，且不落 deadline 终态行（Host 12 次采样命中 5 次） |
| 根因 | `aiosqlite` 的后台线程**无条件**执行已排队语句——取消 `await` 不取消语句。而每一处事务的 `BEGIN` 都写在 `try:` **之外**，取消恰好落在那一行时 `finally` 的 ROLLBACK 一次都不跑，连接上留下**孤儿事务**；紧接着终态写入的 `BEGIN IMMEDIATE` 就抛 `OperationalError`。比 Host 观察到的更严重：孤儿事务**留在连接上**，之后每一次写入（含下一次召回的 admit）都继续抛同一个错 |
| 裁决 | `DECISION-2026-09-09-deadline-in-collection.md`：否掉"在终态写入前查 `in_transaction`"（该属性不走串行队列，可能在孤儿 BEGIN 执行**之前**读到 False，且只救一处）、否掉"把 BEGIN 挪进 try"（try 只在 BEGIN **返回之后**生效）、否掉"关掉取消"（与 deadline 契约冲突）；采纳**把开事务做成不可丢失的原语** |
| 实现 | 新增内部原语 `backends/sqlite_tx.py`（`begin_transaction` / `rollback_transaction`）：BEGIN 单独排队 + `asyncio.shield`，取消时先等它落地、真开了事务就补一条 ROLLBACK 再抛。召回路径（含 6 个 helper 模块）**全部 47 处** `BEGIN*` 改走它，**语句文本逐字不变**；四处裸写的 `except BaseException: ROLLBACK` 改走 `rollback_transaction`。终态写入另加纵深防御。入账之后每一处超时改抛 `TypedRecallDeadlineExceeded(stage)`（9 个阶段名），`stage` 只活在异常对象上、不进任何持久行 |
| 契约面 | 无 DDL 变化（7.4 checksum 不变）；根导出零增减；公共错误类仍是 `TimeoutError` 且 `str(exc)` 仍是 `DEADLINE_EXCEEDED`；终态行形状与入账阶段到期时逐字相同；快照 `public-api-0.6.33.json` 除 `version` 外与 0.6.19 起逐字相同 |
| 测试 | 新增集成 3 项（原语层成对见证、候选收集段到期的确定性构造、50 次 1 ms 预算 soak）；负控：后两项在 0.6.32 基线 worktree 上以 `OperationalError` 失败。快照测试增加 0.6.33 段；schema cutover 版本钉死顺延。全量失败集合与 main 基线（`eec9760`）逐条相同（63 项既有环境失败，`diff` 为空），1622 → 1625 passed / 8 skipped；ruff 计数与基线同为 709 |
| Host 待办 | **不需要改任何调用**。401 RUN-10 §10 第 1 条可改判为已修；`budget:deadline` 的判据不必再刻意把预算耗在入口/入账阶段 |

状态：仅本地候选（分支 `m0633`），未合 main、未构建 wheel、未 push、Host 未 pin。

## 十六、2026-09-09 02:45–03:45（DeepSeek 充值后改用 deepseek-v4-flash）

| 项 | 结果 | Host 提交 |
|---|---|---|
| 语料重测（C01–C04/C06，97 例，flash） | **多提类型率 26/228 = 11.4% ✅（首次达标）**、required 召回 136/136 = 100% ✅、隐私 0 ✅；C04 全部 SETUP_BLOCKED（下条）；C06 `procedure_discover` 调用率 94.7% → 73.7%（hint 与 memory_types 耦合，F-ETR-5）；分析协议 v8 未被本跑道测量 | `786ba9de` |
| **新发现生产回归** | `s5c_store.py` 用户版本封闭白名单 (52,53,54) 未含 F-K1 的 v55 → 所有前瞻注册失败（生产 ProspectiveRuntimeLane 亦受影响）；子代理修复中（派生自迁移链 + 守卫测试） | — |
| A6 尝试 6 | 第 5 轮 `sdk_context_budget_exceeded`：flash 校准倍率 2.01 过保守、超限直接失败关闭 → 修复：flash 按自家证据校准 min(1.25+0.35·ordinal, 1.65)，超限改"强制分页→裁历史组→才失败"，PERSONA/schema 压缩 −309 token | `f161f5a4` |
| A6 尝试 7 | 第 5 轮通过预算；第 6 轮 `primary_message_scope_source_mismatch` 停摆（事件 R：同 Run 第二次 context_route 的预约带 tool_name，v3 校验误判）→ 修复 + 逐条件原因码 | `243369c0` |
| A6 尝试 8 | 进行中（Host 243369c0） | — |
| SDK | 0.6.33 合入（取消时 BEGIN 不再遗留孤儿事务，47 处）；0.6.34（向量阈值 0.45 无余量）进行中；Host pin 0.6.33 待原生运行结束 | SDK `c070119` |
| Manual 旅程 | 按用户"主流程优先"在 T3 暂停，A6 后重跑 | — |

## 十七、2026-09-09 03:45–04:45（HM-TO-A6 第 8 次跑完 24 轮；三个主流程缺陷归因）

- 第 8 次（Host 243369c0，Memory 0.6.31，flash，窗口 32000）24 轮全部走完，`a6_verify.py`：**PASS 10 / FAIL 3 / INCONCLUSIVE 5**（Host `plans/2026-09-08-hm-to-a6/RUN-08-RESULT.md`）。
- 暴露的三个主流程缺陷（均已派独立 Opus 子代理，各自 worktree）：
  1. **F-E2** 同 Run 控制类工具结果（task_scope_search / context_route / context_page_in / tool_search）不可分页、线性累积，T11 在历史全裁剪后仍超 263 token → `sdk_context_budget_exceeded`（A6-3）。
  2. **事件 S** `semantic_correction` 关系候选构建每批 KeyError（21/21），`cognitive_relations` 恒为 0（A6-6/A6-10）。
  3. **召回超时复发** T19–T22 连续 6 次 `context_route_recall_timeout`，导致更正未 supersede、争议未建立（A6-7/A6-8/NC-4）；假设为 Host 仍钉 0.6.31、缺 0.6.33 的 `sqlite_tx.begin_transaction` 孤儿事务修复，第 9 次前钉 0.6.33 验证。
- A6-2 归因为模型行为：flash 用 `run_shell cat` 读 40/47 KB fixture 被 shell 输出上限截断，分页路径未触发；驱动 T6/T8 改为要求 `read_file` 一次读全文。A6-5：flash 未把超长目标逐字写入 goal.set（可用多条 decision.record 备用杠杆）。
- s5c 游标版本 allow-list 修复 + procedure_hint 解耦（F-ETR-5）已合入 Host main（`48617f73`）：迁移链改为从 migrations 文件名发现（`schema_chain.py`），修掉 19 处闭合版本清单（含 `procedure_use_store` 三处潜在缺陷），AST 守卫测试防回归；C04 语料重跑解除阻塞（待 venv 空闲时排程）。
- 第 9 次前置：F-E2 + 事件 S 合入 → 钉 0.6.33/0.6.34 → 重建 bundle → 白天值守跑。
## 十七、2026-09-09（Memory 0.6.34：认知向量准入阈值——同型相对判据 + 绝对护栏，分支 `m0634`）

| 项 | 内容 |
|---|---|
| 缺陷来源 | Host 语料重跑复核 `simple_harness/plans/2026-09-07-corpus-c01-local/RUN-RERUN-FLASH-01-REVIEW.md` §五缺陷 3：同一条中文短 semantic 记忆的两次合理改写余弦 **0.4354 / 0.5839** 跨在 `COGNITIVE_VECTOR_MIN_SCORE = 0.45` 两侧，C01-19 / C06-06 / C06-08 三例仅因此 FAIL，词面通道零重叠、救不回来 |
| 测量 | 用生产同一嵌入器（WeMM-Embedding-2B 本地快照）离线复算：77 例 / 79 次 `context_route` typed recall / 142 条**直接取自证据库**的已激活认知向量。**目标对**（同例、命中请求类型）n=117：min 0.0601 / p05 0.3048 / p10 0.4109 / 中位 0.5738 / max 0.8337；**无关对**（跨例、同请求类型）n=7 562：min −0.0779 / 中位 0.2818 / p90 0.4007 / p95 0.4363 / p99 0.5067 / max 0.6251。离线复现 C01-19 那一对为 0.4343 / 0.5822（与 Host 差 0.001–0.002，设备精度） |
| 裁决 | `DECISION-2026-09-09-vector-score-margin.md`：**(a) 下调常数不可行**——两条分布完全重叠（同义下界 0.0601 < 无关上界 0.6251），任务要求的"下有余量、上在无关之上"的可行域为空；**(c) 让词面兜底不可行**——三例的词面交集全为 0，且 C06-06 / C06-08 的 predicate 本来就是中文（评审表推测的"英文 predicate 稀释"只解释得了 C01-19）；**采纳 (b)** |
| 策略 | `effective(type) = min(0.45, max(0.35, 0.90 × 该类型内已过全部资格门的最高余弦))`，准入 ⟺ `cosine ≥ effective`。等价于「≥ 0.45 或（≥ 0.35 且 ≥ 0.90 × 同类型最高分）」。新增冻结参数 `COGNITIVE_VECTOR_RELATIVE_FLOOR = 0.35` / `COGNITIVE_VECTOR_RELATIVE_RATIO = 0.90`，`COGNITIVE_VECTOR_MIN_SCORE` 仍是 0.45。**相对参照按记忆类型分组**是关键：C06-06 正是 episode/procedure 侧 0.6017 压掉 semantic 侧 0.3690 的形状 |
| 效果 | 并库扫描：目标召回 83.8% → **87.2%**，无关准入 3.74% → **4.01%**（+0.27pp），每次召回候选数 4.82 → 5.13。三个 FAIL 用例按真实候选池离线重算全部转为准入（C01-19 0.4343、C06-06 semantic 0.3690、C06-08 0.4039） |
| 契约面 | 无 DDL（7.4 checksum 不变，已有库不需重建世代）；根导出零增减（新参数与两个纯函数只经 `features.cognitive_vector` 可达）；资格门顺序、四条既有 lane 计分、RRF、`lane_cap`、预算裁剪、退化码与「冷态只退化不失败」全部不变；`effective ≤ 0.45` 恒成立 ⇒ 0.6.33 已准入者判定不变（pin）。审计新增 `terminal_json.cognitive_vector.admission`（两个参数 + 逐类型 `effective_min_score`），**只在真的跑了向量通道的召回上出现** |
| 测试 | 新增单测 8 项（含 121×121 网格逐点等价、`effective ≤ 0.45` 全域扫描）+ 集成 5 项（合成向量精确复现 0.4354 / 0.5839）。快照测试增加 0.6.34 段；schema cutover 版本钉死顺延。全量失败集合与 main 基线（`c070119`，独立 detached worktree）逐条相同（63 项既有环境失败，`diff` 为空），1625 → 1638 passed / 8 skipped |
| Host 待办 | ① **C07 零召回子集必须回归**——本轮 rerun 证据里 `no_recall=true` 与 `privacy_allowed=false` 的用例数为 0，护栏 0.35 只有分布证据、没有端到端证据；② C06-06 类的语义鸿沟仍在（0.3690 离护栏只有 0.019 余量），若要稳住需给 semantic 的向量文本补确定性中文渲染（对标 0.6.26 的 `cognitive_text_supplement`），那会改 `COGNITIVE_TEXT_FORMAT_VERSION` 并整代重建，另行立项；③ 语料回执建议读 `terminal_json.cognitive_vector.admission.effective_min_score` |

状态：仅本地候选（分支 `m0634`），未合 main、未构建 wheel、未 push、Host 未 pin。

## 十八、2026-09-09（Memory 0.6.35：关系端点的分类决定由血缘上最近的已分类祖先承担，分支 `m0635`）

| 项 | 内容 |
|---|---|
| 缺陷来源 | Host 事件 S 备忘 `simple_harness/plans/2026-09-08-hm-to-a6/DECISION-S-RELATION-KEYERROR.md` §3「坑二」/ §8 后继 **F-S1（P0，SDK）**。Procedure 走满三次观测进 `active` 后 head 是 **revision 4，而 `cognitive_classification_decisions` 只有 revision 1**；把它当 `ExistingMemoryTarget` 端点下发，`_resolve_semantic_relation_payload_unlocked` 抛 `MemoryCorruptionError('relation endpoint classification is missing')`，**整批分析死掉**（同批 episode 一起丢）。Host 只能按名扣下所有 Procedure 端点（`sdk_procedure_endpoint_unresolvable`），A6-6 的 Procedure 形态在 0.6.34 上不可达 |
| 根因 | `cognitive_memory_revisions` 只有两个写入点：mutation apply（每 operation 必配分类决定）与 `_copy_cognitive_revision_unlocked`（观测/信号提交，**逐字复制** content/`content_hash`/`effective_privacy_class`/`information_attributes_json`/有效时间/证据 span，只改 `lifecycle_state`，从不重跑分类策略）。所以「每版一行分类」**从来不是本仓的不变量**，0.6.34 把它当成了不变量 |
| 否决 (a) 补写分类行 | ① 伪造审计（该表钉着 policy 四元组 + 逐条证据权威 + 闭合 `decision_json` + `decision_hash UNIQUE`，观测提交根本没跑过分类策略）；② **救不了存量库**（只对将来的提交生效，覆盖存量要迁移/世代重建）；③ 改写 twin 血缘 refs 的 `classification` 一支 |
| 采纳 (b) | **管辖某一版的分类决定 = 血缘上最近的已分类祖先**。查 `MAX(memory_revision) <= 端点 revision`，三条判据：① 恰好一行且 hash 非空，否则 `'…classification is missing'`（**语句逐字不变**）；② **管辖决定必须仍然逐字描述这一版**——决定行被 `decision_hash` 锚定的 `effective_privacy_class`/`effective_attributes_json` 必须等于端点 revision 行的对应两列，**exact 与继承两条路径都查**，否则新增 `'…classification differs'`；③ 继承路径再要求祖先与端点两版 `content_hash` 相等，否则新增 `'…classification lineage differs'`。②③ 是 0.6.34 完全没有的两道校验（它连「这一行是不是仍然描述这一版」都不问）；同时诚实记账：被接受的端点 revision 集合**确实变大了**，这正是修复目的 |
| 契约 | `slices/S3-cognitive-systems-recall.md` 新增 **`§2-补（2026-09-09，0.6.35）`**：Task 2「任何 classification authority 缺失整批拒绝」的适用范围是 mutation **operation** 而非每条 revision；祖先归属规则与两条继承自证条件；**关系端点解析的完整门序**（此前从未入契约）；端点隐私类/属性作为关系记忆分类的下界；F-S2 离线车道口径；F-S1b 未闭合 |
| 契约面 | 零 DDL（7.4 checksum 不变，**已有库不需迁移、不需重建世代**，且**降级安全**：不写新行不加新列）；只改读不改写，观测/信号提交路径一个字节未动、不补任何分类行（用例断言修复后分类行集合仍是 `(1,)`）；端点解析其余各门与顺序不变（Procedure 仍必须 `active`/`reinforced`）；根导出零增减，`public-api-0.6.35.json` 除 `version` 外与 0.6.19 起逐字相同 |
| 测试 | 新增 `tests/integration/test_procedure_relation_endpoint.py` **6 项**：① 事故现场 + 落 1 行 `cognitive_relations` + twin graph 1 条边（**两端**都在节点集合）+ reopen 逐字相同；② typed recall 命中的 exact revision 与**落库的那条边**一致；③ 整条分类链摘掉仍 `missing`，且那样的库**重开即 fail closed**；④ 属性漂移 → `classification differs`；⑤ 内容自洽分叉 → `lineage differs`；⑥ **Prospective 同形**（`TIME_DUE` 到 `TRIGGERED`、head=revision 2）走同一条继承路径并落边。除守卫用例外 5 项基线 `36dac46` 红、本分支绿。全量失败集合与基线**逐条相同**（63 项既有环境失败，`diff` 为空），1638 → **1644 passed** / 8 skipped；`ruff check src tests` 计数与基线同为 **709** |
| 独立评审 | opus 只读评审：**1 条 MUST-FIX**（新不变量与端点门序未入 S3 契约，`S3:51-52` 未被正面回应）已改；**8 条 NIT 全部处置**——继承校验改为比对**被哈希锚定的决定行**并覆盖 exact 路径、两个篡改用例改回全仓惯用的 close→tamper→reopen（第三个不可能这么写，改为显式断言重开即 fail closed）、typed recall 用例改为真正观测修复、twin graph 补 source 端断言、`§4` 口径改写、`§8` 数字内联、新增 Prospective 用例、两次扫描代价记账（不加索引以免动 DDL）。逐条见裁决备忘 §8.1 |
| Host 待办 | **F-S1 只解除了一半，`sdk_procedure_endpoint_unresolvable` 暂不能撤**。另一半是 `check_history_visibility` 对 Procedure 永远 `RECALL_AUTHORITY_STALE`（`backends/history_visibility.py` 写死 `procedure_applicability_fingerprints=frozenset()`）——那是一次**公共 API 扩面**（`core/port.py` + `core/manager.py` + 后端同时改），且会把备忘 §4.1 的 `applied_use_fingerprints` 取舍（保住「用过」、放弃「此刻仍适用」）从"无暴露面"变成"直接决定 `check()` 放不放行"，备忘自己写明必须重新裁定。记为 **F-S1b**，另立版本。今天解除扣留只会把整批打在 `check()` 那一关（备忘 §4.4） |

状态：仅本地候选（分支 `m0635`），未合 main、未构建 wheel、未 push、Host 未 pin。

## 十八、2026-09-09 04:45–06:30（Host 钉 0.6.34；HM-TO-A6 第 9 次跑完；六个主流程事件并行修）

| 项 | 结果 | 提交 |
|---|---|---|
| Memory 0.6.34 | 向量准入相对余量 `min(0.45, max(0.35, 0.90×同类型最佳))`，0.45 成上限（0.6.33 已录取者全保留）；审计 `terminal_json.cognitive_vector.admission` | SDK `36dac46`；Host 钉 `dbf967fc` |
| Memory 0.6.35 | 关系端点分类取最近已分类祖先（观测提交的 Procedure revision 无自身分类行不再判损坏），新增 differs / lineage differs 两道门；S3 §2-补 | SDK `b82187e`（Host 尚未钉） |
| Host 合入 | F-E2 控制类结果压桩（`566011b7`）、事件 S 关系候选 KeyError（`3bdcdb94`）、s5c 迁移链（`48617f73`）、验证器接入回执字段（`e5659e53`） | — |
| **A6 第 9 次** | 24 轮走完：**PASS 9 / FAIL 6 / INCONCLUSIVE 3**（Host `plans/2026-09-08-hm-to-a6/RUN-09-RESULT.md`）。**召回超时 6→0**（0.6.33 生效）、事件 S 生效、A6-7/A6-10 首次 PASS；预算超限 4 次（F-E3）、A6-6 关系形态（事件 T）、T17 refs_outside_scope 循环（事件 U）、T22 争议无通知（事件 V）、一次 76 K token 请求绕过预算 + 调用/回执差 1（事件 W） | — |
| 并行子代理（Opus） | F-E3 分页摘要成本、事件 T、U、V、W、F-S1b（SDK 0.6.36 + Host 解除 Procedure 端点扣留） | — |
- 事件 U 已合入 Host（`db587430`）：`task_scope_update_refs_outside_scope` 拒绝披露可引用 evidence id（范围已链接 ∪ 本 Run 已受理 USER 证据，仅 id 不带哈希/载荷）、当前轮 evidence id、可执行下一步与有界升级；空集时明确终止。**程序侧 followup U-1**：S5b Task 3 / design-freeze §7 把谓词写成「refs ∈ 该 scope 已链接 evidence」，实现已扩为「∪ 本 Run 已受理的 USER 证据」（否则 Run 内新建的 scope 无任何可引用 evidence，用户当轮陈述的目标不可记录）；待下一轮契约修订时同步文字。U-2：`task_scope_search`/`context_route` 的依赖披露里 evidence id 未标注是否可引用。
## 十九、2026-09-09（Memory 0.6.36：离线车道可以提交自己召回时所用的 Procedure 适用性指纹，分支 `m0636`）

| 项 | 内容 |
|---|---|
| 缺陷来源 | 0.6.35 裁决备忘 §6 第 2 条 / §9 **F-S1b（P0，SDK）**，再往上是 Host 事件 S 备忘 §3「坑一」/ §4.4 / §7。0.6.35 修好了关系端点**解析**，但 Host 的 `check()` 走 `check_history_visibility`，而 `backends/history_visibility.py` 写死 `procedure_applicability_fingerprints=frozenset()`（注释：*never reuse old runtime fingerprints*），于是任何 Procedure 来源恒 `RECALL_AUTHORITY_STALE` → `history_source_stale` → 整批 `analysis_candidate_no_longer_visible`。Host 只能继续扣下所有 Procedure 端点 |
| 两难 | 那句注释**是对的**：`check_history_visibility` 拿到的是 `DisclosureContext` 而不是 `RecallContext`，让它去读已存召回请求里的指纹，等于让一次任意晚的复核继承一次任意早的运行时状态。要解决的是：既不让前台复核 replay 旧指纹，又让一条**本来就没有活着的 Run** 的离线车道能用它召回时所用的同一份事实完成复核 |
| 否决 (a)(b) | (a) 让 `check_history_visibility` 自己去读那条召回请求的指纹——正是注释禁止的行为，且对**所有**调用方生效，复核退化成回放；(b) 加一个「跳过 Procedure 适用性」的布尔开关——把「为什么可以放行」变成「调用方要求不检查」，收据里只剩一个 `True`，与事件 S 备忘 §4.4 批评的「装好引信的洞」同型 |
| 采纳 (c) | **显式 provenance 的 attestation + Memory 自己的观测审计佐证**。新增根导出 `ProcedureApplicabilityProvenance`（闭合枚举，1 个成员 `applied_use_fingerprints`）/ `ProcedureApplicabilityAttestation`（构造期要求已排序、去重、1..256 条）/ `ProcedureApplicabilityReceipt`；`check_history_visibility`（协议 + facade + 后端）加**可选** `procedure_applicability`。**这个标签不被信任**：放行还要求该 memory 上至少一条 `outcome='success' AND attributable=1` 的 `procedure_observations` 携带逐字相同的指纹——那是 Memory 自己的 append-only、不可变触发器保护、open 时逐字复核的表（实测：改一列则库不可开）。分工说准：既有类型权威门已经把调用方钉死在 Memory 自己存的值上，新增这一条把「被绑定过」收紧为「被一次成功可归因的使用背书过」。其余各门一条不少 |
| 缺省与 fail closed | 不提交 ⇒ 逐字等于 0.6.35，快照 `to_json()` 连 `procedure_applicability` 这个键都不出现（两个哈希因此也逐字相同）；提交给当前输入观察入口 ⇒ `history_current_input_rejects_procedure_applicability`；非 canonical 类型 ⇒ `TypeError`；facade **只在真的提交时才转发该 kwarg**，早于 0.6.36 的后端照常工作 |
| 收据 | 快照在 `schema_version` **之后**追加 `procedure_applicability`（provenance / `attestation_hash` / `fingerprint_count` / **逐条命中的 binding hash**，按调用方绑定顺序），attestation 同时计入 `request_hash`；既有 7 参位置式构造逐字不变 |
| 重新裁定 | 事件 S 备忘 §4.1 要求 F-S1 解除时必须重裁 `applied_use_fingerprints`（保住「用过」、放弃「此刻仍适用」）。**裁定：可以作为复核依据**——① 保住的那一半被**收紧到「成功且可归因地用过」**（「等于 Memory 自己存的值」本由既有类型权威门保证）；② 佐证来源被链锚定（篡改则库不可开）；③ 它只是复核不是授权，apply 时端点仍被逐门重解析。**残余（记账不修）**：工具改名/换签/被撤的 Procedure 前台召回消失，离线车道仍可提交它作为关系端点；该边仍带 exact revision，遗忘/suppression/冲突/状态各门照常生效。彻底消除需把「适用性快照」持久化成可离线重算的事实，记 **F-S1c** |
| 契约 | `slices/S3-cognitive-systems-recall.md` 新增 **`§2-补2（2026-09-09，0.6.36）`** 七条：缺省即 0.6.35；唯一入口与 provenance 语义；作用面只到 `HistoryRecallBinding`（当前输入入口显式拒绝）；三条放行判据与「其余各门一条不少」；收据形状；**重新裁定 §2-补.6 的取舍**；F-S1 两条全部闭合（§2-补.7 作废） |
| 契约面 | 零 DDL（7.4 checksum 不变，不加表/列/索引，已有库不需迁移或重建；**降级安全**——新键只在返回值里、不落库）；写路径与前台 `execute_typed_recall` 一个字节未动；`cognitive_relations` 与 twin graph 投影、收据视图、分类决定链复核均未改。`public-api-0.6.36.json` 是 0.6.19 以来**第一次**真正的根导出扩张：只多三个名字，`removed_public_methods` 与 `migrations` 逐字未变 |
| 测试 | 新增 `tests/integration/test_history_procedure_applicability.py` **9 项**（Procedure 走满三次真实观测进 `active` → 真实指纹公开 typed recall → `HistoryRecallBinding`，与 Host 分析车道逐字同形）：缺省仍 stale 且快照无新键；带 attestation 即可见、收据四项齐全、两次调用逐字相同、`request_hash` 与不带的那次不同；指纹对不上不放行；**判据 ③ 单独量出**（篡改三条观测的指纹 → 同一 attestation 变 stale，并断言这样的库重开即 fail closed）；同批 semantic 绑定不受影响也不进命中清单；当前输入入口拒绝 + 非 canonical `TypeError`；`generation=STALE` 仍不可见；canonical 形状 fail closed；facade 只在提交时转发。基线 `b82187e` 上整文件收集即失败（公共面新增，预期）。全量失败集合与基线**逐条相同**（63 项既有环境失败，`diff` 为空），1644 → **1653 passed** / 8 skipped；`ruff check src tests` 与基线同为 **709** |
| 独立评审 | opus 只读评审，逐条处置见裁决备忘 §9.1 |
| Host 同步 | Host 分支 `worktree-f-s1b` 同步解除 `sdk_procedure_endpoint_unresolvable`，按**能力探测**（根导出三个类型 + facade 有 `procedure_applicability` 参数）而不是版本号，因此同一份 Host 代码在 0.6.34/0.6.35 上继续扣留。召回时的指纹被持久进候选快照，`check()` 用**同一份**构造 attestation。**F-S1 两条至此全部闭合**，A6-6 的 Procedure 形态端到端可达（Host 实测：`cognitive_relations` 1 行 `applies_to` 指向观测提交出来的那一版 head，孪生图出现对应的边） |

状态：仅本地候选（分支 `m0636`），未合 main、未构建 wheel、未 push、Host 未 pin。
