# Human Memory Program 续接记录（2026-09-07，taiwan Mac 接手）

> 接手自 [HANDOFF-2026-09-07.md](HANDOFF-2026-09-07.md)。本文件记录 09-07 本机会话的事实与用户决定；后续段落按时间追加，不改写既有记录。

## 用户 09-07 决定（覆盖旧执行方式中的对应项）

1. 模型：两边（App `llm_runtime.json` 与语料评分跑道）都用 `gpt-5.6-luna`（svtun `/v1`）。
2. 推进顺序：先 HM-AC-8 质量门主线，再原生 r8 收口，最后 S5c/S6。
3. 240 语料遇产品缺陷：先跑完，按根因聚合后批量修，只重跑受影响条目。
4. **转主干开发**：所有提交直接进各仓 `main` 并推送；不再以 feature 分支为收敛点。
5. 费用/耗时不设上限，跑完汇报。

## 三仓并入 main（09-07）

| 仓 | 动作 | main HEAD |
|---|---|---|
| Host `simple_harness` | `origin/feat/human-memory-primary-candidate`（631 提交）+ 本机候选分支并入；无冲突 | 见 git log（09-07 多次提交） |
| Memory SDK | `origin/feat/human-memory-procedure-current-input-successor`（0.6.19 源，102 提交）并入；`ARCHITECTURE/*.md` 三处冲突取并集 | `52910b0`（含 0.6.20 修复） |
| Harness SDK | `origin/feat/nullable-tool-schema`（0.7.10 源，74 提交）fast-forward | `fd12e7d` |

未并入的 Host leaf 分支（20 个，均为 09-06/07 WIP：corpus-c05/c06/c07/c08/complete-dispatch、prospective-*、procedure-*、audit-access、cognitive-controls、selected-short-runtime、terminal-audit-identity 等）留待按需评估后并入；Harness 另有 run-operation-audit(+5) 等 4 个未并入。

## 本机环境

- Host `backend/.venv`：uv sync，H0.7.10 / M0.6.20 / S0.3.13；installed target `.local-test-evidence/2026-09-07/installed-h0710-m0620-s0313`（成员逐字节与 wheel 一致 174/92/116）。
- 那台 Mac 的原始证据包（3.0 GB）解压于 Host `.local-test-evidence/from-other-mac/`。
- 主树 `.env` BASEURL 补 `/v1`；评分 worker 凭据路径改为 `host_root.parent/simple_harness/.env`（可 `CORPUS_CREDENTIAL_ENV_FILE` 覆盖）。

## 质量门主线进展

### 首次完整真实模型执行与根因

Host 候选树上 C01-10 三次执行（r1 180s 截止被终止；r2 首次完整闭环但 3 次召回全空；r3 spike 修词法门后命中），详见 Host `plans/2026-09-07-corpus-c01-local/RESULTS.md`。

根因：Memory SDK `sqlite_v5.py` typed recall 候选门/confirmation 门用 `[\w㐀-鿿]+` 切词，`\w` 已含汉字，中文查询退化为整句匹配；向量通道硬编码 `cognitive_vector_unavailable`。中文长期语义记忆在无 entity/scope/时间约束时全部 `recall_no_eligible_memory`。

### Memory 0.6.20 候选

- 源 `52910b0c`：新增 `features.lexical.typed_recall_query_terms`（原词项 + CJK 二字组合），两处门改用；3 项中文正/负控 + 0.6.20 公共 API 快照（表面与 0.6.19 一致）通过；typed recall 相关套件 73 项无回归。全量 1572 项中 63 项失败与基线（0.6.19 源）完全相同，属环境/历史红（缺那台 Mac 本地 fixture 等）。
- wheel `0cc065d9…`，自 git archive 离线构建，SOURCE_DATE_EPOCH=提交时间；Host `sdk_candidate.py`/pyproject/uv.lock 同步，已提交 main。
- 主树正式复验 C01-10：召回命中 `recall-item:e7be274c…:1`，回答「取件→提交→复查」与 gold 一致，类型预测精确 semantic（extra=0）。

### 240 批量（run-01）

当前 prepare_batch 支持 69 例：C01 全 20、C07 全 20、C05 {04,07,08,09,14,20}、C08 {01,06,11,18}、C09 除 13 外 19。其余 171 例 prepare 直接拒绝（`corpus_batch_requires_exact_supported_id`），需补 setup 适配。

批量驱动 `scripts/run_corpus_batch.py`（串行、每例独立进程组、6GiB/900s、失败不重试）；复核材料抽取 `scripts/corpus_review_extract.py`。run-01 结果见下文追加段。

## 原生 r8 两处故障的现状（读那台 Mac 记录）

- 后置 Provider 清理 KeyError：修复 f0f72650 已在 main；r9 起未再观察到。
- 短期投影 MemoryCorruptionError：根因为 `register_group` 漏入场 terminal 来源，修复 fa7580b0 + FTS/VECTOR 0bedaa87 已在 main；r10/r12 原生真实通过。
- 因而第 2 阶段的内容变为「在当前 main 组合上重跑一次完整原生旅程」，而非重新定位这两个 bug。
