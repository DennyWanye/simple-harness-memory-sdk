# Human Memory Program 本机续接记录

最后更新：2026-09-05。原始范围仍由 `plan.md`、`acceptance.md` 与各 slice 定义；本记录不替代机器验收。

## 同步与执行顺序

- [x] 安全快进 Host `a3f1afb94e8a42713fdd82710bf75ab2fd62eddd`、Harness `f5fe0dc7e8c5b521444e01c40cab176f3666c627`、Memory `78d61926e15a8b0b8f49971472e2f5ef2b25f60a`。
- [x] Service 当前 `47f372adc641d8d3516599dd21cb94cf5955d6a7` 与远端 main 相同，保留原分支。
- [x] 读取原始 program、S5b 批准的 acceptance、A14/A15 与当前交接；执行模式：Host 修复串行，Memory 恢复修复和 S3 consumer runner 在独立 worktree 推进。
- [x] 核验 Host vendored Harness 0.7.1 / Memory 0.6.2 / Service 0.3.12 的 SHA-256 与安装来源，替换本机旧 Harness 0.6.4 / Memory 0.5.2。
- [ ] S5b 正确性整改、当前生产入口与 UI 重测、独立终审、机器门。
- [ ] S3 typed-recall 完整 consumer 验收；S5c；S6 与 program 全部 MUST AC 终验。

## 迁移边界

本机执行交接指定 `status --run-dir .../verification/r4-s5b`，实际返回 `USAGE_ERROR: run-dir 缺少 plan-test-run.json`。
r3/r4 的部分文字产物已被上游纳入 Git，但机器账本与 UI 启动脚本不在本机；不能凭这些文字恢复旧 PASS 或重建旧 receipt。
本机重新编译七场景 manifest，初始状态全部 `NOT_RUN`，没有导入历史 PASS。原始 r3 排障与 r4 六轮结果仅作交接中报告的历史。

当前本地记录：Host `.local-test-evidence/2026-09-05/human-memory-resume/verification/r5-local/`。
新日志、review JSON、receipt 等原始证据只写 ignored 目录，不提交。远端拉取改动了旧日志 ignore 例外，导致本机旧证据暴露为 untracked；保留原文件，不将其混入提交。

## 本机基线与发现

| 核验 | 本机结果 | 证据（Host 本地相对路径，前缀为 `.local-test-evidence/2026-09-05/human-memory-resume/`） |
|---|---|---|
| S5b milestone、acceptance matrix、v46 cutover | 32 passed | `s5b-smoke.log` |
| S5b closure / analysis / effect 故障矩阵 | 127 passed，1 failed | `s5b-fault-matrix.log` |
| 连接错误分类测试修复后 | 9 passed | `post-turn-invoker-retest.log` |
| S3 typed recall / graph / audit / public API 最小集 | 62 passed | `memory-s3-smoke.log` |
| 前端类型检查 | PASS | `frontend-typecheck.log` |
| 当前 Tauri debug build | PASS，13 warnings | `tauri-build.log` |

故障矩阵唯一红：测试向关闭的 loopback 端口发请求却继承 HTTP 环境，得到 ReadTimeout 而非 ConnectError。
显式 `trust_env=False` 后得到预期的未发送错误；只修测试环境隔离，不放宽生产重试断言。

独立审查（Popper，非执行者自审）结论 `REQUEST_CHANGES`：

1. **S5B-IR-01 / P1**：重领读到 reserved 后旧执行者 handoff，恢复 UPDATE 可将 handed_off 错标 not_sent，再发送第二次。
   先新增事件屏障回归，旧代码稳定 `2 != 1`；修复为 reserved 状态 CAS，CAS 失败零新调用；独立复审接受，Host `f0133ac8` 提交后 35 passed。
2. **S5B-IR-02 / P1**：同 principal 后续 batch 越过 apply 失败的旧 batch，旧结果恢复时因 base_revision 过期被拒；两个 job applied 但只物化一条记忆。
   Memory 已修复并独立复审接受，合入 `d70d98c`；两条 accepted、两条物化且总 Provider 调用为 2。
3. **S5B-IR-03 / P2**：Host 合法 no_mutation 附带 closure_reason，而 Memory exact-key 校验拒绝。已修复：保留合法理由，不可用响应仍 rejected。

审查复现与结构化 findings 保存于 `independent-review/`。Memory 源码全量 1124 passed / 9 skipped，
独立复审另跑 24 条。0.6.3 candidate 版本/API 快照及决定性回归 16 passed；exact-wheel 尚待验证，不推进 SHIPPABLE/交付收口。

## 原始 plan 进度纠偏

独立静态审计（Hegel）确认：HANDOFF-2026-09-05 把 S3 Task 5–7 描述成程序性/前瞻性状态机不准确。
状态机属于 Task 3；Task 5 是 typed recall，Task 6 是展示图谱，Task 7 是审计与质量/wheel 门。
当前三者均有实现与测试，不能重做已有能力，也不能据此标记整个 S3 终验完成。

真正待办：TC-HM-13 runner 仍在校验 wheel 后无条件返回 NOT_RUN/BLOCKED，未执行 391 public Manager cells + 10 source cells；
240 条 corpus 尚为 `AI_DRAFT_UNREVIEWED`，未完成原 plan 要求的 corpus 分类修订、独立人工冻结及真实质量/性能门。
`register_principal_owner` 已存在，不再作为 Memory 开发欠账。文档各处旧状态将在对应切面验收后同步校正。

## 运行前置与尚未验证

本机 `.env` 的 Provider 为 `kimi-k3`，只做了 HTTP 200 基础可达检查，尚无本轮生产链成功证据。
API key 仅注入进程内，未读取 Keychain。WeMM 固定 revision
`bbd6cd4bf52cfc6716f752a2df80b2706720bd95` 已恢复，权重 SHA-256
`e1a1ad752808c26965aa97d37bf4a9bca71d838513f41e83790cb1e71cac6e59`；实际 MPS 生成 2048 维向量，L2 norm=0.99826。

当前 Tauri 打包为独立 `SimpleHarness HM R5.app`，隔离 userdata 冷启 schema v46、身份连接成功。
真实原生 UI 在既有会话绑定 `gpt-5.5` 后收到“这次桌面消息已经收到。”；
session `76a36af2-2682-5391-abb4-9ffd67a3c4a3`、root `3ec3cdad3e8f5baa824540f75bd3e1c3`。
这是 chat_v2 的 S8 通道证据，不是 A14 foreground queue.enqueue/S1 证据；S1 在新 wheel 上另跑。

保留两项失败：kimi-k3 缺 context window metadata，发送前被拒；空态模型面板保存空 session_id
后关闭，首发仍使用全局模型。后者交 S6 Task 1（启动唯一主对话 hydrate）一起处理，需覆盖设置就绪、
ACK/错误反馈与首发所选模型。另记 SDK diagnostics async snapshot 被同步调用的 warning，不解释模型绑定失败。

当前结论：**续接进行中，S5b 与 program 均未完成；旧机器结论不作为本机已验证证据。**
