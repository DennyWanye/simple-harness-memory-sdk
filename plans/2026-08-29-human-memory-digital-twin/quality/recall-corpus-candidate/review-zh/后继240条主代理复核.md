# 后继240条语料主代理复核

最后更新：2026-09-06。结论：接受 `successor-12x20-r4-20260906` 作为下一步实现评估适配器的中文语义基准。一个AI子代理完成草拟与审查，主代理已逐条复核全部240条并复查修订；不再要求用户逐条审核。这是AI两层设计审查，不是独立人工冻结，也不是模型质量通过或可直接运行的完整数据包。

[完整中文MD总索引](successor-12x20/总索引.md)提供12类链接。静态核对12类各20条、240个唯一ID连续无重号，13个成员hash与总索引一致；批次指纹：`2a69f5712991f665eafed67d5457f7d6b8484885ab186927b57f5a7014c576d1`。

## 复核后已改正

- 任务类20个首轮输入与22条固定后续消息分离；真实候选返回且当前轮结束后才能发后续消息，不把后续答案提前交给模型。内部受控预览与正式resume/执行授权区分。
- 时间类显式scenario_clock一致约束Host、SDK裁决及Provider日期；季度题使用9月30日真实已发生设定，不能在9月6日回顾未来事件。任务C05-15明确2026年8月公开日期，运行器不得从答案反推设置。
- 最近历史题显式user/assistant角色与顺序，经真实历史入口进入Context，不用一个贴有角色标签的用户字符串冒充已验证历史。
- 非本人用途政策独立于gold和私密记录是否存在，统一对全部240条生效。C12-20正式世界固定存在虚构私密记录；“不存在”的世界仅为另列补充对照，不暗增样例/调用/分母。
- 保留原required/no-recall/hard/privacy/task-search类别与阈值；修正来源别名。模型显式类型选择已接入组合Host，但不能把Host默认集合或成功记录当作全部模型提案。

## 实现与实测仍需完成

这些MD的setup是语义要求，不是已存在的公共fixture API。下一步需要独立设置层、Provider输入隔离、真实工具调度、四层trace与原指标计分。初始输入不能含gold、正确ref、库内隐藏事实或记录存在性；可信政策和真实历史由相应适配器提供。缺接口必须报告设置不足，不能私写SDK表或伪造授权receipt补齐。

必须分别记录模型提案、Host允许范围、实际执行及最终交付；失败/超时attempt也要观测，不能只统计成功结果。零查询不能自动证明后台gate执行，后台门未经过则标NOT_EXERCISED；按原规则保留适用样例与分母，不以无查询直接凑hard/privacy通过。

实际运行仍需两次独立模型试次、性能和真实Context预算验证。当前零模型调用，没有质量分数；401矩阵完整证明仍另列。正文中较早设计文件对模型类型接口的描述是当时历史，当前状态以总索引更新节和本复核为准。

旧24×10 JSONL、manifest、原spec/metrics未改；新240不与旧240或首批12例相加。后继机器数据产生时必须重新绑定其字节与本批次语义，不能冒用旧corpus hash。此次仅提交Markdown，不提交原始测试证据。
