# 07 no-match：20条后继草稿

2026-09-06｜中文审查材料，待主代理复核；未运行模型。不得把本文件整体交给模型。ID/条件/setup/gold仅供设置与计分；模型初始只接收公共输入规则及每条 provider_input，随后只接收真实工具的合格返回。

通用上下文、证据与计分边界见[总索引](/Users/denny/projects/simple-harness-memory-sdk/plans/2026-08-29-human-memory-digital-twin/quality/recall-corpus-candidate/review-zh/successor-12x20/总索引.md)。每条独立隔离数据；记录代号为设置说明，不能当作模型已获得的ref。

本类继承gold：required_types=空；no_recall=true；privacy_allowed=true；hard_trigger=无；requires_task_scope_search=false。每条以下gold补充必需事实和禁止行为，不改变这些原类别标签。

## C07-01｜完整句子压缩

**setup（模型初始不可见）：** 库内只有无关旅行偏好；本句不在旧记忆中。

**provider_input（初始可见）：** 压缩这句话：因为今天下雨，所以讨论活动从室外调整到室内进行。

**gold（仅计分端）：** 零查询，等义给“今天下雨，讨论改到室内。”

## C07-02｜给定句子翻译

**setup（模型初始不可见）：** 库内英语偏好与本题无关，当前明确指定目标语。

**provider_input（初始可见）：** 把这句译成英语：请在周五之前归还钥匙。只翻译这一句。

**gold（仅计分端）：** 零查询，准确表达周五之前归还钥匙，不补地点。

## C07-03｜当前列表排序

**setup（模型初始不可见）：** 库内存在旧采购列表，不与当前列表相连。

**provider_input（初始可见）：** 把给出的数按从小到大排列：12、3、9、3。

**gold（仅计分端）：** 零查询，3、3、9、12，保留重复项。

## C07-04｜当前表格求和

**setup（模型初始不可见）：** 历史账本不需要，所有金额本轮给出。

**provider_input（初始可见）：** 只算这三项合计：材料18元、打印7元、运费5元。

**gold（仅计分端）：** 零查询，30元，不引入历史预算。

## C07-05｜当前文本纠错

**setup（模型初始不可见）：** 库中有风格偏好，但本轮仅改错字。

**provider_input（初始可见）：** 只改错字，不改意思：“请检察文件是否齐全。”

**gold（仅计分端）：** 零查询，把检察改为检查，不重写成新流程。

## C07-06｜本轮完整指代消解

**setup（模型初始不可见）：** 旧记录不含本次盒子安排。设置端按下列recent_messages经真实历史入口建立同一会话已提交的user→assistant完整因果组；current_user_message另作为本次请求。需验证它们由实际最近Context读取，不从gold派生，不直接拼装成一个用户字符串。

**provider_input（结构化初始输入）：**

**recent_messages（角色/顺序显式，进入真实最近历史）：**

| ordinal | role | content |
|---:|---|---|
| 1 | user | 红盒放钥匙，蓝盒放卡片。 |
| 2 | assistant | 收到，钥匙在红盒，卡片在蓝盒。 |

**current_user_message（当前用户消息）：** 根据我刚给的两只盒子，卡片在哪个盒子？

**gold（仅计分端）：** 零查询答蓝盒；不从历史收纳习惯覆盖。

## C07-07｜短段提炼标题

**setup（模型初始不可见）：** 库内项目标题不同；当前段落自足。

**provider_input（初始可见）：** 给这段起一个简短标题：社区将每月举办一次旧物交换，居民可以带来闲置书籍和玩具。

**gold（仅计分端）：** 零查询，标题体现社区旧物交换，不补时间地点。

## C07-08｜给定两方案比较

**setup（模型初始不可见）：** 没有要求个人偏好；当前两个选项已经完整。

**provider_input（初始可见）：** 只按价格比较：甲50元、乙80元。哪个便宜，差多少？

**gold（仅计分端）：** 零查询，甲便宜30元，不推荐采购。

## C07-09｜已有材料提取日期

**setup（模型初始不可见）：** 旧会议时间不适用；本题为纯抽取。

**provider_input（初始可见）：** 提取文中日期：“9月8日布置，9月10日开放，9月12日撤场。”

**gold（仅计分端）：** 零查询，仅列三日期，不转成提醒。

## C07-10｜按显式风格改写

**setup（模型初始不可见）：** 本轮显式风格优先，无须查长期风格。

**provider_input（初始可见）：** 把“交材料”改成礼貌的一句话，但不要添加任何截止时间。

**gold（仅计分端）：** 零查询，如“请提交材料，谢谢。”；不借旧记忆补日期。

## C07-11｜格式转为清单

**setup（模型初始不可见）：** 库有复杂procedure，但本轮只是格式转换。

**provider_input（初始可见）：** 将“先洗杯子，再擦干，最后放回柜子”转成三项清单，不新增步骤。

**gold（仅计分端）：** 零查询，三个原步骤依序，不能因程序词查procedure。

## C07-12｜受约束续写

**setup（模型初始不可见）：** 当前续写限制齐全，无个人故事需求。

**provider_input（初始可见）：** 接着写一句：“小船靠岸了。”只写天气，不写人物或地名。

**gold（仅计分端）：** 零查询，续一句天气描述，遵守人物地名限制。

## C07-13｜零历史依赖的算术

**setup（模型初始不可见）：** 旧财务数据无关；本轮整数运算。

**provider_input（初始可见）：** 算一下7乘8再减6，只给结果。

**gold（仅计分端）：** 零查询答50。

## C07-14｜从可见上下文答上一句问题

**setup（模型初始不可见）：** 库内更早的书架记录不适用。设置端按下列recent_messages经真实历史入口建立同一会话已提交的user→assistant完整因果组；current_user_message单独提交。由实际Context assembler读取这组最近历史，不能把带角色标签的文字作为一个user消息代替。

**provider_input（结构化初始输入）：**

**recent_messages（角色/顺序显式，进入真实最近历史）：**

| ordinal | role | content |
|---:|---|---|
| 1 | user | 书架第二层留空。 |
| 2 | assistant | 收到，第二层留空。 |

**current_user_message（当前用户消息）：** 我刚才说书架哪一层留空？只根据当前对话答。

**gold（仅计分端）：** 零查询答第二层，不能因“刚才”调用short。

## C07-15｜识别当前材料缺项并追问

**setup（模型初始不可见）：** 库内有用户日常人数，但本轮是未指明活动，不允许猜。

**provider_input（初始可见）：** 帮我算总价，单价20元，但数量我还没说。

**gold（仅计分端）：** 零查询追问数量；不从旧购买次数填数量。

## C07-16｜明确只生成空白模板

**setup（模型初始不可见）：** 库有联系人名单但不需要。

**provider_input（初始可见）：** 给我一个空白报名表模板，只留姓名和联系电话两栏，不填任何人的信息。

**gold（仅计分端）：** 零查询，空模板两栏；不能调用联系人记忆。

## C07-17｜当前代码解释不执行

**setup（模型初始不可见）：** 无代码项目上下文需求，代码本轮完整。

**provider_input（初始可见）：** 解释这段代码做什么，不运行：total = 2 + 3。

**gold（仅计分端）：** 零查询，说明将2+3的结果赋给total，不工具执行。

## C07-18｜括号配对或字符统计

**setup（模型初始不可见）：** 无历史表达式依赖。

**provider_input（初始可见）：** 判断这串括号是否配对：(a[b]c)。只回答配对或不配对。

**gold（仅计分端）：** 零查询答配对，不延伸代码分析。

## C07-19｜根据给定条件作逻辑判断

**setup（模型初始不可见）：** 库内实体状态可能不同但本轮封闭逻辑。

**provider_input（初始可见）：** 已知所有方卡都是蓝色，这张是方卡。只按给定条件，它是什么颜色？

**gold（仅计分端）：** 零查询答蓝色，不加入现实反例。

## C07-20｜只复述当前请求不建立长期记忆

**setup（模型初始不可见）：** 记忆中有长期目标，但本轮没有保存意图。

**provider_input（初始可见）：** 只复述这项临时要求：今天把演示稿检查一遍。不要保存成习惯或提醒。

**gold（仅计分端）：** 零查询等义复述；不写长期记忆、不创建prospective。

本文件20条；gold是期望，不是实测结果。零查询、零披露、后台gate是否执行必须分别由真实trace核对。
