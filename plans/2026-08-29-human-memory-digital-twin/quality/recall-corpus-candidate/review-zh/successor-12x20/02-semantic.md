# 02 semantic：20条后继草稿

2026-09-06｜中文审查材料，待主代理复核；未运行模型。不得把本文件整体交给模型。ID/条件/setup/gold仅供设置与计分；模型初始只接收公共输入规则及每条 provider_input，随后只接收真实工具的合格返回。

通用上下文、证据与计分边界见[总索引](/Users/denny/projects/simple-harness-memory-sdk/plans/2026-08-29-human-memory-digital-twin/quality/recall-corpus-candidate/review-zh/successor-12x20/总索引.md)。每条独立隔离数据；记录代号为设置说明，不能当作模型已获得的ref。

本类继承gold：required_types=semantic；no_recall=false；privacy_allowed=true；hard_trigger=无；requires_task_scope_search=false。每条以下gold补充必需事实和禁止行为，不改变这些原类别标签。

## C02-01｜维护能力决定个人脚本语言

**setup（模型初始不可见）：** A：本人长期优先用Python维护个人数据脚本；B：一次演示Ruby，无偏好改变。

**provider_input（初始可见）：** 我要做读表格再导出的小工具。结合我平时会维护的东西，选个最顺手的语言并说一句原因。

**gold（仅计分端）：** 取得A建议Python并绑定维护能力，不靠通用推荐或B推断。

## C02-02｜通勤约束决定课程形式

**setup（模型初始不可见）：** A：本人通勤每天两小时且周末不便外出，长期更愿选可回看的线上课。

**provider_input（初始可见）：** 我想选一门周末课程。按我一贯的时间安排，直播到场和录播哪种更合适？

**gold（仅计分端）：** 取得A倾向可回看录播，解释通勤/外出约束；不虚构具体周末安排。

## C02-03｜可复用饮食口味用于非医疗菜单

**setup（模型初始不可见）：** A：本人喜欢清淡口味、不喜很甜；非健康限制。

**provider_input（初始可见）：** 给我搭一份普通茶点，按我一直喜欢的口味。候选是原味苏打饼、糖霜蛋糕和无糖茶。

**gold（仅计分端）：** 取得A选苏打饼/无糖茶，不将口味包装成医学建议或疾病事实。

## C02-04｜可访问性需求决定文档呈现

**setup（模型初始不可见）：** A：本人读屏使用者，要求说明避免只靠颜色区分。

**provider_input（初始可见）：** 我有红绿状态的任务列表。按我的阅读需要，怎么表达状态才方便我看？

**gold（仅计分端）：** 取得A建议补文字状态，不能只换颜色；无需检索使用读屏的过去事件。

## C02-05｜旅行偏好与当次经历区分

**setup（模型初始不可见）：** A：本人旅行喜欢留半天自由时间；B：上次赶行程是一次团体安排。

**provider_input（初始可见）：** 下次短途旅行，行程排满还是留空档更符合我平时的习惯？

**gold（仅计分端）：** 取得A建议空档，不从B推断本人喜欢排满；不订票。

## C02-06｜预算取向用于礼物筛选

**setup（模型初始不可见）：** A：本人送礼重实用，预算通常控制在200元内。

**provider_input（初始可见）：** 给朋友挑礼物，按我一直的取向比较：180元保温杯、350元摆件。

**gold（仅计分端）：** 取得A推荐符合实用/预算的杯子，不把350元自动视为获准支出。

## C02-07｜学习习惯决定讲解组织

**setup（模型初始不可见）：** A：本人学新概念习惯先看一个小例子再读抽象定义。

**provider_input（初始可见）：** 我要开始理解递归，按我最容易吸收的方式安排讲解顺序。

**gold（仅计分端）：** 取得A先例子后定义；不得只凭通用教学习惯命中标签。

## C02-08｜低带宽约束用于素材交付

**setup（模型初始不可见）：** A：本人经常移动网络办公，偏好体积小、可断点下载的素材。

**provider_input（初始可见）：** 我要把几份设计素材留给自己路上看。结合我的使用条件，原图包和压缩预览该先准备哪个？

**gold（仅计分端）：** 取得A先压缩预览并保留后续原图途径；不实际上传。

## C02-09｜工作节奏决定议程结构

**setup（模型初始不可见）：** A：本人上午集中做深度工作，协调讨论放下午。

**provider_input（初始可见）：** 按我通常的工作节奏，把独立写作和团队对齐分配到上下午。

**gold（仅计分端）：** 取得A上午写作/下午对齐，不误当今天已有日历事件。

## C02-10｜风险偏好用于可逆方案比较

**setup（模型初始不可见）：** A：本人试新工具先保留可撤回旧版本，不喜欢一步覆盖。

**provider_input（初始可见）：** 试一个笔记工具时，我会更接受先复制试用还是直接替换全部旧资料？结合我以往明确的取向。

**gold（仅计分端）：** 取得A选复制试用；不检索发布procedure、不执行迁移。

## C02-11｜长期设备条件决定附件格式

**setup（模型初始不可见）：** A：本人阅读设备不便打开宏，常用纯文本或PDF。

**provider_input（初始可见）：** 一份阅读资料给我自己存档，带宏表格和PDF哪个更适合我的设备条件？

**gold（仅计分端）：** 取得A选PDF并说明兼容；不虚构设备型号。

## C02-12｜安静偏好决定活动时间建议

**setup（模型初始不可见）：** A：本人居住环境夜间需安静，休闲练习安排白天。

**provider_input（初始可见）：** 按我家里一贯的环境限制，今晚练鼓还是明天下午练更合适？

**gold（仅计分端）：** 取得A选下午，理由只述已存安静约束，不披露额外家庭细节。

## C02-13｜个人关注点决定摘要重点

**setup（模型初始不可见）：** A：本人看产品更新最关注数据导出和兼容性。

**provider_input（初始可见）：** 更新有新配色、离线导出、旧文件兼容三项。按我平时关注的重点，摘要先说什么？

**gold（仅计分端）：** 取得A突出导出和兼容，不把配色排首。

## C02-14｜家庭空间约束用于收纳方案

**setup（模型初始不可见）：** A：本人书桌可用宽度80厘米且不愿占地放柜。

**provider_input（初始可见）：** 按我家里的空间条件，桌上小收纳架和落地宽柜哪个更适合？

**gold（仅计分端）：** 取得A推荐桌面小架并保留尺寸需核查，不编造购买规格。

## C02-15｜稳定专业背景决定解释深度

**setup（模型初始不可见）：** A：本人会基本SQL但未学过查询优化。

**provider_input（初始可见）：** 给我解释索引，深浅按你记得的我的基础来。

**gold（仅计分端）：** 取得A从SQL使用衔接索引概念，不假定优化专家、不回顾学习episode。

## C02-16｜明确反偏好排除默认推荐

**setup（模型初始不可见）：** A：本人明确不喜欢以排行榜作为唯一选择依据。

**provider_input（初始可见）：** 比较两个读书应用，按我的长期取向，先看排名还是先看离线阅读能力和导出限制？

**gold（仅计分端）：** 取得A先功能约束，不绝对排除参考排名。

## C02-17｜有条件偏好结合当前用途

**setup（模型初始不可见）：** A：本人手机上爱短列表，电脑复盘时偏好细表格。

**provider_input（初始可见）：** 我现在用电脑做月度复盘，按我一贯要求，结果该怎样排版？

**gold（仅计分端）：** 取得A选复盘细表格，不因默认短回复偏好遗漏用途条件。

## C02-18｜同主体多个偏好需要联合满足

**setup（模型初始不可见）：** A：本人工作说明要中文；B：本人重视能复制的操作示例。

**provider_input（初始可见）：** 做一份给我自己的工具说明，结合我一直在意的两点，你会怎样组织？

**gold（仅计分端）：** 取得两个semantic事实，中文并有可复制示例；不捏造具体procedure。

## C02-19｜明确事实优先于未确认推断

**setup（模型初始不可见）：** A：本人直接声明优先选离线方案；B：系统曾推测本人偏好云端，未确认。

**provider_input（初始可见）：** 就我明确说过的习惯看，离线笔记和全云端笔记哪个方向更适合？

**gold（仅计分端）：** 用A而非未确认B，不把推断当显式用户偏好。

## C02-20｜信息不足时只应用已存限制不补造

**setup（模型初始不可见）：** A：本人只明确说过文具不超过100元；没有颜色偏好。

**provider_input（初始可见）：** 按我已有的偏好，在80元蓝笔和90元黑笔里推荐，并说明你还不知道什么。

**gold（仅计分端）：** 取得预算约束，两者均满足；明确颜色未知，不为了定唯一项虚构偏好。

本文件20条；gold是期望，不是实测结果。零查询、零披露、后台gate是否执行必须分别由真实trace核对。
