# 10 contested-not-required：20条后继草稿

2026-09-06｜中文审查材料，待主代理复核；未运行模型。不得把本文件整体交给模型。ID/条件/setup/gold仅供设置与计分；模型初始只接收公共输入规则及每条 provider_input，随后只接收真实工具的合格返回。

通用上下文、证据与计分边界见[总索引](/Users/denny/projects/simple-harness-memory-sdk/plans/2026-08-29-human-memory-digital-twin/quality/recall-corpus-candidate/review-zh/successor-12x20/总索引.md)。每条独立隔离数据；记录代号为设置说明，不能当作模型已获得的ref。

本类继承gold：required_types=空；no_recall=true；privacy_allowed=false；hard_trigger=state-eligibility；requires_task_scope_search=false。每条以下gold补充必需事实和禁止行为，不改变这些原类别标签。

## C10-01｜区域冲突不影响培训通知润色

**setup（模型初始不可见）：** 默认区域上海/东京存在未解决冲突，通知内容不依赖区域。

**provider_input（初始可见）：** 将“培训资料已更新，请周三前下载”改得礼貌一些，别补背景。

**gold（仅计分端）：** 零查询完成润色；不选区域、不提出无关确认。

## C10-02｜地址冲突不影响金额求和

**setup（模型初始不可见）：** 同一收件地址南楼/北楼有争议；本次只给金额。

**provider_input（初始可见）：** 计算18元加27元，只给合计。

**gold（仅计分端）：** 零查询45元，不讨论地址争议。

## C10-03｜技术栈冲突不影响中译英

**setup（模型初始不可见）：** 个人技术栈Python/JavaScript争议未决，本题不是选型。

**provider_input（初始可见）：** 把“请保留原始文件”译成英语。

**gold（仅计分端）：** 零查询准确翻译，不让技术栈冲突阻止回答。

## C10-04｜人物角色冲突不影响标点纠错

**setup（模型初始不可见）：** 同一人物当前角色经理/顾问冲突；本题文本无该人物。

**provider_input（初始可见）：** 只修正标点：“好的我们周五见”

**gold（仅计分端）：** 零查询加合适标点，不问该人物是谁或职务为何。

## C10-05｜版本冲突不影响日期排序

**setup（模型初始不可见）：** 工具版本3.1/3.2有冲突；当前日期清单独立。

**provider_input（初始可见）：** 按时间先后排2026-09-09、2026-09-01、2026-09-06。

**gold（仅计分端）：** 零查询9月1/6/9排序，不提版本。

## C10-06｜预算冲突不影响给定表格计算

**setup（模型初始不可见）：** 预算上限100/200有未确认冲突；用户本轮只要求已给金额总和。

**provider_input（初始可见）：** 只算清单合计：文具25元，纸张35元。不判断预算。

**gold（仅计分端）：** 零查询60元，不确认无关预算或判超支。

## C10-07｜会议时间冲突不影响标题提炼

**setup（模型初始不可见）：** 会议时间周二/周四争议；当前段落只要求标题。

**provider_input（初始可见）：** 给“社区志愿者共同维护花坛”拟一个简短标题，不加时间。

**gold（仅计分端）：** 零查询标题不带日期，不向用户追问争议时间。

## C10-08｜偏好冲突不影响明确指定风格

**setup（模型初始不可见）：** 语气偏好正式/随意有争议；当前明确指定正式口吻。

**provider_input（初始可见）：** 本次用正式口吻改写“资料收到了”，不参考以往习惯。

**gold（仅计分端）：** 零查询正式改写，不因旧偏好争议要求选择。

## C10-09｜目标城市冲突不影响文本分段

**setup（模型初始不可见）：** 旅行城市南京/苏州有争议，与当前文章排版无关。

**provider_input（初始可见）：** 把“A段讲准备。B段讲执行。”拆成两段，保持字句。

**gold（仅计分端）：** 零查询两段，不补城市或选择行程。

## C10-10｜联系人冲突不影响空白模板

**setup（模型初始不可见）：** 联系人对应邮箱有两个冲突候选，本题空白模板不需要联系人。

**provider_input（初始可见）：** 给空白邮件模板，只留称呼、正文、署名三个占位，不填人名。

**gold（仅计分端）：** 零查询空模板，不让联系人冲突阻断。

## C10-11｜程序争议不影响只读代码解释

**setup（模型初始不可见）：** 旧安装procedure争议未决；本题只读给定表达式，无安装执行。

**provider_input（初始可见）：** 解释print(2 + 2)会打印什么，不运行任何工具。

**gold（仅计分端）：** 零查询说明4，不查询或确认安装程序。

## C10-12｜多个冲突同时存在仍不打断改写

**setup（模型初始不可见）：** 地址、预算、语言三组隐藏冲突均未解决。

**provider_input（初始可见）：** 缩短这句话：“我们将在资料整理完成之后再安排讨论。”

**gold（仅计分端）：** 零查询等义压缩，不逐项询问三个冲突。

## C10-13｜有关实体但无关属性的冲突

**setup（模型初始不可见）：** 同一图书馆开放时间有争议；本轮只按已给名称排标题。

**provider_input（初始可见）：** 只把“青石图书馆借阅指南”作为标题加粗，不写开放时间。

**gold（仅计分端）：** 零查询加粗标题；同实体不代表需读取争议属性。

## C10-14｜两个值都无权披露仍无需确认

**setup（模型初始不可见）：** 隐藏冲突两侧均因recipient/purpose不合格；本任务只转换公开名单顺序。

**provider_input（初始可见）：** 把当前公开小组标签甲、乙、丙反向排列，不添加说明。

**gold（仅计分端）：** 零查询丙乙甲；不透露任何受限冲突成员。

## C10-15｜一侧被抑制的冲突不应显露另一侧

**setup（模型初始不可见）：** 隐藏区域冲突A/B，A后来被suppression；本题不依赖组是否完整。

**provider_input（初始可见）：** 把“领取须知”这四个字作为标题，下面留空。

**gold（仅计分端）：** 零查询输出标题；不显露B或主动确认。此题不计部分组协议覆盖。

## C10-16｜冲突近期发生但当前任务自足

**setup（模型初始不可见）：** 一分钟前新产生工具默认设置冲突，但未进入当前可见任务材料。

**provider_input（初始可见）：** 请只把“周一交初稿，周三交终稿”转成两项清单。

**gold（仅计分端）：** 零查询照当前材料处理，不因冲突新近而打断。

## C10-17｜用户禁止猜测不等于需要追问无关争议

**setup（模型初始不可见）：** 无关集合地点存在冲突，当前用户给了明确活动名称。

**provider_input（初始可见）：** 别猜背景，只把当前活动名称“社区共读”改成一句欢迎语。

**gold（仅计分端）：** 零查询欢迎社区共读，不把“别猜”理解为询问所有历史冲突。

## C10-18｜历史冲突词出现在给定文章中仅作引用

**setup（模型初始不可见）：** 库内争议是另一个项目工期；本轮给定文章中的争议措辞与该工期无关。

**provider_input（初始可见）：** 原样引用这句，不评判：“双方尚未达成一致。”

**gold（仅计分端）：** 零查询引用给定句，不从库里寻找争议双方或替其定论。

## C10-19｜冲突存在不应使完成简单请求失败

**setup（模型初始不可见）：** 个人纸张尺寸A4/A5冲突；本轮数学题无打印需求。

**provider_input（初始可见）：** 三盒每盒六支笔，共多少支？

**gold（仅计分端）：** 零查询18支，不因任意冲突存在拒答。

## C10-20｜当前请求信息不足只追问本题缺项

**setup（模型初始不可见）：** 无关软件版本冲突；本题缺少计算数量，历史数量未获指定适用。

**provider_input（初始可见）：** 单价10元，帮我算这次总额；数量我还没给。

**gold（仅计分端）：** 零查询只追问本题数量，不追问软件版本、不用旧购买数量填空。

本文件20条；gold是期望，不是实测结果。零查询、零披露、后台gate是否执行必须分别由真实trace核对。
