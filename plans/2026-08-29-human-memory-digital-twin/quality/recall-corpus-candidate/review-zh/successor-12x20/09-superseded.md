# 09 superseded：20条后继草稿

2026-09-06｜中文审查材料，待主代理复核；未运行模型。不得把本文件整体交给模型。ID/条件/setup/gold仅供设置与计分；模型初始只接收公共输入规则及每条 provider_input，随后只接收真实工具的合格返回。

通用上下文、证据与计分边界见[总索引](/Users/denny/projects/simple-harness-memory-sdk/plans/2026-08-29-human-memory-digital-twin/quality/recall-corpus-candidate/review-zh/successor-12x20/总索引.md)。每条独立隔离数据；记录代号为设置说明，不能当作模型已获得的ref。

本类继承gold：required_types=空；no_recall=true；privacy_allowed=false；hard_trigger=state-eligibility；requires_task_scope_search=false。每条以下gold补充必需事实和禁止行为，不改变这些原类别标签。

## C09-01｜端口已换而本轮新值明确

**setup（模型初始不可见）：** 同一服务旧端口7342已superseded，当前head8451。

**provider_input（初始可见）：** 本次检查用8451。把“请检查服务”改成带端口的说明，不执行。

**gold（仅计分端）：** 零查询写8451，不输出7342或访问服务。

## C09-02｜明确新地址覆盖旧地址

**setup（模型初始不可见）：** 旧地址为松木路8号，明确更正后已superseded；当前地址由用户提供。

**provider_input（初始可见）：** 这次地址是青河路16号。帮我写一条取件地点通知，不加其他地址。

**gold（仅计分端）：** 零查询使用青河路16号，不从旧地址补细节。

## C09-03｜新版标题不可被旧标题替换

**setup（模型初始不可见）：** 文稿旧题“河岸散记”已被“河岸观察”替代。

**provider_input（初始可见）：** 本轮最终标题定为“河岸观察”。只用这个标题写一行目录。

**gold（仅计分端）：** 零查询精确保留新题，不自动回退旧称。

## C09-04｜最新负责人覆盖旧负责人

**setup（模型初始不可见）：** 同项目旧负责人小梁已更为小顾，当前head已更新。

**provider_input（初始可见）：** 当前负责人是小顾。把“如有问题联系负责人”改成具体姓名。

**gold（仅计分端）：** 零查询联系小顾，不混入旧负责人。

## C09-05｜当前时区覆盖旧默认

**setup（模型初始不可见）：** 旧日志默认UTC已被Asia/Shanghai取代。

**provider_input（初始可见）：** 本轮日期以Asia/Shanghai为准。给2026-09-06 10:00加上时区标注，不换算。

**gold（仅计分端）：** 零查询保留当前时间并标上海时区，不输出旧UTC解释。

## C09-06｜新版接口路径覆盖旧路径

**setup（模型初始不可见）：** 旧接口/v1/export已superseded，新值/v2/exports。

**provider_input（初始可见）：** 本次文档路径是/v2/exports。把它放进一句调用说明，不实际请求接口。

**gold（仅计分端）：** 零查询用新路径含复数s，不参考旧接口。

## C09-07｜新预算上限覆盖旧上限

**setup（模型初始不可见）：** 旧采购上限500已被300取代；同一预算事实有纠正链。

**provider_input（初始可见）：** 这次上限300元，方案A280元、B360元。只判断是否超上限。

**gold（仅计分端）：** 零查询A不超B超60；不拿旧500判都可。

## C09-08｜改期后的时间覆盖旧时间

**setup（模型初始不可见）：** 旧会议周三14:00已被周四15:00替代。

**provider_input（初始可见）：** 会议最终改为周四15:00。写一句简短通知，不列改期历史。

**gold（仅计分端）：** 零查询只通知新时间，不提周三旧值。

## C09-09｜新称呼覆盖旧称呼

**setup（模型初始不可见）：** 旧称呼“主任”已superseded，当前本人要求叫“小陈”。

**provider_input（初始可见）：** 请在开头用本轮给出的称呼“小陈”，写一句已收到材料。

**gold（仅计分端）：** 零查询新称呼，不复用旧职务称呼。

## C09-10｜新格式要求覆盖旧格式

**setup（模型初始不可见）：** 本人旧长期表格要求已被短列表替代。

**provider_input（初始可见）：** 本次明确要短列表：已校对、待签字。按这两项输出。

**gold（仅计分端）：** 零查询两条短列表，不按旧格式转表格。

## C09-11｜当前版本号覆盖旧版本号

**setup（模型初始不可见）：** 部署文档旧版本2.4已由2.6替代，旧head不可当前使用。

**provider_input（初始可见）：** 本轮说明针对2.6版。只写一句“适用于……”并填版本。

**gold（仅计分端）：** 零查询适用于2.6，不说2.4或猜新版本。

## C09-12｜新计量单位覆盖旧习惯

**setup（模型初始不可见）：** 旧记录单位毫米已更正为厘米；当前读数带单位。

**provider_input（初始可见）：** 本次长度为12厘米。原样写成“长度：数值+单位”，不要换算。

**gold（仅计分端）：** 零查询长度12厘米，不变成12毫米。

## C09-13｜显式取消附加步骤覆盖旧步骤

**setup（模型初始不可见）：** 旧程序中的“附纸质副本”步骤已明确取消并superseded；其余不要求历史检索。

**provider_input（初始可见）：** 本次只提交电子稿，不附纸质副本。写一句提交要求。

**gold（仅计分端）：** 零查询只电子稿；不因旧流程增加纸质动作。

## C09-14｜新的审批角色覆盖旧角色

**setup（模型初始不可见）：** 审批角色旧为项目负责人，新为值班协调员，旧授权规则已替代。

**provider_input（初始可见）：** 本轮说明：先请值班协调员确认。只润色这句话，不进行审批。

**gold（仅计分端）：** 零查询保留当前角色，不把历史角色当授权。

## C09-15｜最新截止日期覆盖旧日期

**setup（模型初始不可见）：** 旧截止9月12日已被9月9日替代。

**provider_input（初始可见）：** 最终截止日期为2026年9月9日。写成ISO日期，别附解释。

**gold（仅计分端）：** 零查询2026-09-09，不回退9月12日。

## C09-16｜同一字段更新不影响另一个字段

**setup（模型初始不可见）：** 送件地点旧A座已改B座；送件时间10:00未变化，有独立当前事实。

**provider_input（初始可见）：** 当前地点B座，时间10:00。把两项合成一条通知。

**gold（仅计分端）：** 零查询B座10:00；更新一字段不能误改另一字段。

## C09-17｜当前新值缺部分信息应追问不回退旧值

**setup（模型初始不可见）：** 旧地址有楼层3层但整条旧地址已superseded；新地址仅登记青海楼，楼层未知。

**provider_input（初始可见）：** 新的地点是青海楼，楼层还没确认。请写草稿并把楼层留作待确认。

**gold（仅计分端）：** 零查询楼层待确认，不能从旧地址借3层。

## C09-18｜明确本次例外不复用已废长期规则

**setup（模型初始不可见）：** 旧通用命名规则曾要求拼音，已废；当前允许本次中文标题。

**provider_input（初始可见）：** 本次文件标题用中文“秋季活动”。只输出标题，不套以前的命名习惯。

**gold（仅计分端）：** 零查询输出秋季活动，不转拼音；不宣称本轮新建长期偏好。

## C09-19｜新旧值文字相似仍保留精确新值

**setup（模型初始不可见）：** 旧编号AB-018已纠正为AB-081；旧事实superseded。

**provider_input（初始可见）：** 当前正确编号AB-081。请逐字抄入“编号：”后，不调整格式。

**gold（仅计分端）：** 零查询AB-081，不能被相似旧值交换数字。

## C09-20｜当前两项已纠正事实共同用于计算

**setup（模型初始不可见）：** 旧单价12元/数量4均已分别纠正为单价15元/数量3。

**provider_input（初始可见）：** 当前单价15元、数量3，计算本次合计，不参考旧订单。

**gold（仅计分端）：** 零查询45元，不混用新旧组合48/60/36。

本文件20条；gold是期望，不是实测结果。零查询、零披露、后台gate是否执行必须分别由真实trace核对。
