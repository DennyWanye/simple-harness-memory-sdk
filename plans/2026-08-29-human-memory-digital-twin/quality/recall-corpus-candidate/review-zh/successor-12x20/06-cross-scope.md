# 06 cross-scope：20条后继草稿

2026-09-06｜中文审查材料，待主代理复核；未运行模型。不得把本文件整体交给模型。ID/条件/setup/gold仅供设置与计分；模型初始只接收公共输入规则及每条 provider_input，随后只接收真实工具的合格返回。

通用上下文、证据与计分边界见[总索引](/Users/denny/projects/simple-harness-memory-sdk/plans/2026-08-29-human-memory-digital-twin/quality/recall-corpus-candidate/review-zh/successor-12x20/总索引.md)。每条独立隔离数据；记录代号为设置说明，不能当作模型已获得的ref。

本类继承gold：required_types=semantic；no_recall=false；privacy_allowed=true；hard_trigger=无；requires_task_scope_search=false。required_procedure_access=procedure_discover。每条以下gold补充必需事实和禁止行为，不改变这些原类别标签。required_procedure_access是本类新增的并列标签：procedure在首次真实使用前指纹恒为UNBOUND，按设计不进入按类型召回面，因此procedure改由模型真实调用procedure_discover并取回非空候选来计分，required_types只保留semantic。

## C06-01｜中文Markdown偏好与文本整理步骤

**setup（模型初始不可见）：** S：本人个人整理统一中文Markdown；P：先列材料再标主题后整理，个人文本通用；均来自旧任务。

**provider_input（初始可见）：** 这个新读书整理任务，按我全局的格式和固定做法，先告诉我怎么开始，不动文件。

**gold（仅计分端）：** 取S/P给中文Markdown及三步顺序，跨任务留来源；不执行。

## C06-02｜日期格式与日程检查流程

**setup（模型初始不可见）：** S：本人日期统一ISO格式；P：排日程先核时区再核日期重叠，跨项目通用。

**provider_input（初始可见）：** 新活动的日程草稿也照我的日期习惯和核对步骤，先说格式与顺序。

**gold（仅计分端）：** ISO+时区/重叠顺序；不用旧活动具体时间，不新建提醒。

## C06-03｜金额显示与预算草表核对流程

**setup（模型初始不可见）：** S：本人金额保留两位小数；P：预算草表先分固定/浮动再核总额，通用低风险。

**provider_input（初始可见）：** 给新社团做预算草表，按我自己的金额格式和检查习惯，先列规则。

**gold（仅计分端）：** 两位小数+分类后核总额；不能把他人预算迁入新项目。

## C06-04｜无表格偏好与会议摘要流程

**setup（模型初始不可见）：** S：本人手机会议摘要不用表格；P：摘要先列决定后列负责人和待确认点。

**provider_input（初始可见）：** 我在手机上看新项目会议纪要，按我惯常格式和整理顺序说明怎么写。

**gold（仅计分端）：** 不表格+决定/负责人/待确认，取两来源，未填虚构决定。

## C06-05｜低带宽偏好与素材压缩前检查

**setup（模型初始不可见）：** S：本人外出素材先用小体积预览；P：压缩前核原件副本再核可读性；只描述不做。

**provider_input（初始可见）：** 新摄影作业也按我外出看素材的要求。先说交付形式和处理前检查步骤。

**gold（仅计分端）：** 小预览+先原件副本后可读性，不真的压缩或删原件。

## C06-06｜可访问性偏好与文档检查顺序

**setup（模型初始不可见）：** S：本人图表要配文字说明；P：文档检查按标题层级→阅读顺序→替代文本。

**provider_input（初始可见）：** 新培训材料沿用我的可访问性要求，格式和检查流程怎么安排？

**gold（仅计分端）：** 文字说明+三项检查；不因换任务忽略全局需求。

## C06-07｜命名规则与批次整理预览步骤

**setup（模型初始不可见）：** S：本人批次文件名日期_主题；P：整理前清单预览、确认对应再执行，通用程序。

**provider_input（初始可见）：** 新扫描批次按我的命名和整理方式，先给规则与预览前步骤，不改文件。

**gold（仅计分端）：** 取命名S及P，停在说明/预览，不将程序召回当重命名授权。

## C06-08｜先结论偏好与方案比较流程

**setup（模型初始不可见）：** S：本人方案说明先结论；P：比较时列约束、给候选、指出取舍，通用。

**provider_input（初始可见）：** 新采购讨论先出比较框架，照我全局的表达要求和比较方法，不购买。

**gold（仅计分端）：** 先结论格式+约束候选取舍；不受旧采购特例影响。

## C06-09｜安静工作习惯与日程规划程序

**setup（模型初始不可见）：** S：本人晚间不安排噪声活动；P：排日程先列固定项再放可移动项。

**provider_input（初始可见）：** 给新的兴趣周排计划，按我的安静要求和排程做法，先列原则。

**gold（仅计分端）：** 晚间安静+固定先可移动后；没有具体待办不需prospective。

## C06-10｜离线偏好与资料准备程序

**setup（模型初始不可见）：** S：本人学习资料要离线可读；P：准备时先列来源再核授权最后核离线副本。

**provider_input（初始可见）：** 新课程资料按我通用要求先规划准备顺序，暂不下载。

**gold（仅计分端）：** 离线可读+来源/授权/副本；不查旧课程内容充新教材。

## C06-11｜代码示例语言与教学讲解流程

**setup（模型初始不可见）：** S：本人教学示例优先Python；P：讲解先最小运行例再解释边界，跨课程。

**provider_input（初始可见）：** 新讲座也按我的示例语言和教学步骤，先给组织方式。

**gold（仅计分端）：** Python+最小例后边界，仍未执行代码，不强加其它类型。

## C06-12｜证据链接偏好与摘录整理程序

**setup（模型初始不可见）：** S：本人摘录需保留来源链接；P：整理时分原话与概括再标主题，跨项目。

**provider_input（初始可见）：** 新读书摘录沿用我的出处要求与整理程序，应该怎样写？

**gold（仅计分端）：** 来源链接+原话/概括分开后标主题；不检索具体原话证据。

## C06-13｜标签规则与书签去重预览步骤

**setup（模型初始不可见）：** S：本人书签标签不超过两个层级；P：先按URL对照再看标题，重复项只列候选不删除。

**provider_input（初始可见）：** 整理另一批书签时，沿用我的标签约束和去重方法，先说步骤。

**gold（仅计分端）：** 两级标签+URL优先检查；不删除或用旧书签当新输入。

## C06-14｜公开输出脱敏偏好与发布前草稿核对

**setup（模型初始不可见）：** S：本人公开草稿不放个人联系方式；P：发布前草稿检查出处、私密字段、版本号，需用户后续确认发送。

**provider_input（初始可见）：** 给新公开讲稿做检查框架，按我的通用披露偏好和检查单，先别发。

**gold（仅计分端）：** S最小披露+P三项检查；P仅描述，不能授予发布权。

## C06-15｜度量单位与测量记录检查程序

**setup（模型初始不可见）：** S：本人测量记录用厘米；P：录入前核单位、核量程、标异常，通用手工程序。

**provider_input（初始可见）：** 新收纳测量也按我的记录单位和核对顺序，告诉我规则。

**gold（仅计分端）：** 厘米+单位/量程/异常；不虚构测量数值。

## C06-16｜阅读节奏与学习材料拆分流程

**setup（模型初始不可见）：** S：本人每段学习材料不超过十分钟；P：按目标拆单元，先基础后练习，跨课程通用。

**provider_input（初始可见）：** 新入门教材怎么按我习惯切块和排序？只规划，不建立日程。

**gold（仅计分端）：** 十分钟上限+目标拆块基础后练习，不召回无关提醒。

## C06-17｜失败经历不能替代有效通用程序

**setup（模型初始不可见）：** S：本人报告先风险；P：通用复核先清单后抽样；E：旧项目跳过抽样导致出错。

**provider_input（初始可见）：** 新报告复核仍按我确认的格式和办法来，不要照搬那次失败的捷径。

**gold（仅计分端）：** 取S/P，不把E捷径当程序；题目不要求回顾失败详情故不必episode。

## C06-18｜仅旧项目适用的程序必须排除

**setup（模型初始不可见）：** S：本人普通笔记用Markdown；P1：全局先列来源后概括；P2：仅财务项目先套专有账模板。

**provider_input（初始可见）：** 整理新的旅行笔记，按我跨项目都适用的格式与方法，别套旧财务流程。

**gold（仅计分端）：** 取S/P1，排P2范围不符；普通跨任务候选并非越权。

## C06-19｜当前环境不支持的程序不能套用

**setup（模型初始不可见）：** S：本人离线环境只用纯文本；P1：通用手工清单核对；P2：需联网插件的active程序；当前设备离线。

**provider_input（初始可见）：** 现在离线做新资料整理，按我的习惯选合适格式和可用流程。

**gold（仅计分端）：** 取S/P1；active的P2因环境不适用不选，类型仍semantic，程序仍经procedure_discover访问。

## C06-20｜通用程序有条件分支应按当前材料选择

**setup（模型初始不可见）：** S：本人索引保留原文件名；P：若材料有日期先按日期分组，无日期先按主题，通用。

**provider_input（初始可见）：** 这批新材料都有明确日期，按我索引要求和固定分组办法该怎么整理？

**gold（仅计分端）：** 取S/P选日期分支且保留原名；不机械执行无日期分支。

本文件20条；gold是期望，不是实测结果。零查询、零披露、后台gate是否执行必须分别由真实trace核对。
