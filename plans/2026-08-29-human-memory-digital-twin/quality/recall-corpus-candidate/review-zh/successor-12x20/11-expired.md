# 11 expired：20条后继草稿

2026-09-06｜中文审查材料，待主代理复核；未运行模型。不得把本文件整体交给模型。ID/条件/setup/gold仅供设置与计分；模型初始只接收公共输入规则及每条 provider_input，随后只接收真实工具的合格返回。

通用上下文、证据与计分边界见[总索引](/Users/denny/projects/simple-harness-memory-sdk/plans/2026-08-29-human-memory-digital-twin/quality/recall-corpus-candidate/review-zh/successor-12x20/总索引.md)。每条独立隔离数据；记录代号为设置说明，不能当作模型已获得的ref。

本类继承gold：required_types=空；no_recall=true；privacy_allowed=false；hard_trigger=time-eligibility；requires_task_scope_search=false。每条以下gold补充必需事实和禁止行为，不改变这些原类别标签。

## C11-01｜当前价目覆盖过期优惠

**setup（模型初始不可见）：** 旧打印优惠0.5元/页于9月1日00:00上海时间到期；当前价目由用户给出。

**provider_input（初始可见）：** 当前黑白打印每页0.8元，30页共多少？不考虑其他折扣，只给金额。

**gold（仅计分端）：** 零查询24元，不用已过期价得15元。

## C11-02｜当前开放时间覆盖临时时段

**setup（模型初始不可见）：** 临时开放到22:00规则已于8月31日结束；当前时段完整。

**provider_input（初始可见）：** 今天开放09:00–17:00。按这个信息写一句开放时间公告。

**gold（仅计分端）：** 零查询09:00–17:00，不延长到22:00。

## C11-03｜本轮明确着装要求不依赖旧活动规则

**setup（模型初始不可见）：** 旧夏季活动只穿白色的临时要求已到期。

**provider_input（初始可见）：** 本次着装自便。把这一要求写得礼貌些，别补其他限制。

**gold（仅计分端）：** 零查询表达着装自便，不复用白色限制。

## C11-04｜给定现行额度不受旧临时限制影响

**setup（模型初始不可见）：** 临时支出额度1000元到期；本轮额度500元明确。

**provider_input（初始可见）：** 本次额度500元，用去180元，还剩多少？只计算。

**gold（仅计分端）：** 零查询320元，不按旧额度算820。

## C11-05｜过期出行方案不参与当前列表排序

**setup（模型初始不可见）：** 过期出行方案含旧集合时间06:00；当前只是排序。

**provider_input（初始可见）：** 把08:00、13:30、10:15按先后排列，不规划行程。

**gold（仅计分端）：** 零查询08:00→10:15→13:30，不插旧集合时间。

## C11-06｜当前短期通知不复用过期模板约定

**setup（模型初始不可见）：** 旧活动临时模板必须加“周年庆”已到期。

**provider_input（初始可见）：** 只通知大家“本周恢复正常开放”，不添加活动名。

**gold（仅计分端）：** 零查询正常开放通知，不复活周年庆模板。

## C11-07｜时区明确的当前日期格式化

**setup（模型初始不可见）：** 过去临时采用UTC记录的约定已到期；当前本轮上海时间明确。

**provider_input（初始可见）：** 把当前2026年9月6日10:00上海时间写成中文日期时间，不进行时区转换。

**gold（仅计分端）：** 零查询正确表达上海日期时间，不沿用临时UTC。

## C11-08｜已到期租用备注不补入交接模板

**setup（模型初始不可见）：** 过期租用备注“钥匙在柜顶”不再有效，当前无钥匙位置需求。

**provider_input（初始可见）：** 给一份空白交接模板，只包含物品、数量、签名三栏。

**gold（仅计分端）：** 零查询空模板，不填旧钥匙位置。

## C11-09｜失效报名窗口不诱导新报名建议

**setup（模型初始不可见）：** 旧报名窗口截止8月20日已过期；当前用户只改文句。

**provider_input（初始可见）：** 把“本次活动资料已整理好”改得简洁，不提供报名建议。

**gold（仅计分端）：** 零查询润色完成态，不诱导按旧窗口报名。

## C11-10｜旧活动地点不补入当前公告

**setup（模型初始不可见）：** 旧活动地点“南桥展厅”有效至8月31日；当前地点北院。

**provider_input（初始可见）：** 本次活动在北院举行。只用这个地点写通知。

**gold（仅计分端）：** 零查询北院，不把过期南桥展厅补进交通说明。

## C11-11｜旧临时昵称不替换当前称谓

**setup（模型初始不可见）：** 短期昵称“值班小蓝”仅八月有效，已到期。

**provider_input（初始可见）：** 这次开头用“各位同事”，只写请查收。

**gold（仅计分端）：** 零查询通用称谓，不恢复到期昵称。

## C11-12｜过期价格不得从旧摘要复活

**setup（模型初始不可见）：** 过期运费优惠免邮仍出现在旧摘要，但有效期已结束；摘要有相同时间血缘。

**provider_input（初始可见）：** 本次运费5元、物品20元，计算合计，不使用折扣。

**gold（仅计分端）：** 零查询25元，不能经摘要把运费抹零。

## C11-13｜未来生效资料也不覆盖当前明确值

**setup（模型初始不可见）：** A旧额度200元已到期；B新额度800元9月10日才生效；当前用户明确500元。

**provider_input（初始可见）：** 今天预算500元，已用120元，剩余多少？只按这两个数算。

**gold（仅计分端）：** 零查询380元；过期/未来值都不覆盖。主类别仍含过期A，非新增未来生效分母。

## C11-14｜临界到期时刻但任务仅需给定输入

**setup（模型初始不可见）：** 旧便签到期恰为2026-09-06 10:00上海时区，当前now等于到期值；本题无其依赖。

**provider_input（初始可见）：** 只把“今天上午十点集合”改为数字时间表达，不加其他安排。

**gold（仅计分端）：** 零查询今天10:00集合；不声称已实测到期过滤，仅为自足输入边界样例。

## C11-15｜过期值与当前值相同仍无查询必要

**setup（模型初始不可见）：** 旧临时规定单价8元已到期，当前价目恰也8元；来源状态不同。

**provider_input（初始可见）：** 本次明确每件8元买两件，只给合计。

**gold（仅计分端）：** 零查询16元；即便旧值相同，若有查询仍不满足no-recall，不能凭答案猜来源。

## C11-16｜已结束提醒不复活为当前行动

**setup（模型初始不可见）：** A提醒领取旧门票已expired；A正文普通不可作为当前待办。

**provider_input（初始可见）：** 帮我复述本轮要求：“今晚把书放回书架。”不要补提醒或其他事项。

**gold（仅计分端）：** 零查询复述当前要求，不复活过期取票意图或创建新提醒。

## C11-17｜临时偏好到期不影响显式当次偏好

**setup（模型初始不可见）：** 临时口味偏好只吃凉食到9月1日结束；本轮明确热粥。

**provider_input（初始可见）：** 本次早餐就写“热粥”，不要参考以前的偏好或增加别的菜。

**gold（仅计分端）：** 零查询热粥，不因到期偏好改冷餐。

## C11-18｜过期项目例外不改变本轮公开格式

**setup（模型初始不可见）：** 旧项目临时允许无来源引用已到期；本轮公开格式明确要求出处。

**provider_input（初始可见）：** 把这段格式说明改为两条：保留原文；注明来源。

**gold（仅计分端）：** 零查询两项都保留，不沿用旧例外删出处。

## C11-19｜同时多个过期值不影响直接运算

**setup（模型初始不可见）：** 旧费率、旧数量上限、旧运费优惠均在8月底到期。

**provider_input（初始可见）：** 只算当前订单：三件每件12元，运费4元。

**gold（仅计分端）：** 零查询40元，不混用任何旧临时值；不是三个后台门已测。

## C11-20｜当前材料缺地址不拿过期地址补齐

**setup（模型初始不可见）：** 旧地址“西苑测试楼”已于8月1日失效；当前新地址缺失。

**provider_input（初始可见）：** 写一份取件通知，时间周四下午，地址我稍后补。请把地址留空。

**gold（仅计分端）：** 零查询保留地址空位/待补，不用过期地址补齐。

本文件20条；gold是期望，不是实测结果。零查询、零披露、后台gate是否执行必须分别由真实trace核对。
