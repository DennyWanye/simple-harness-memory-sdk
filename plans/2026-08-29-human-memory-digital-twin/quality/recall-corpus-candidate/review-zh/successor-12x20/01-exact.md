# 01 exact：20条后继草稿

2026-09-06｜中文审查材料，待主代理复核；未运行模型。不得把本文件整体交给模型。ID/条件/setup/gold仅供设置与计分；模型初始只接收公共输入规则及每条 provider_input，随后只接收真实工具的合格返回。

通用上下文、证据与计分边界见[总索引](/Users/denny/projects/simple-harness-memory-sdk/plans/2026-08-29-human-memory-digital-twin/quality/recall-corpus-candidate/review-zh/successor-12x20/总索引.md)。每条独立隔离数据；记录代号为设置说明，不能当作模型已获得的ref。

本类继承gold：required_types=semantic；no_recall=false；privacy_allowed=true；hard_trigger=无；requires_task_scope_search=false。每条以下gold补充必需事实和禁止行为，不改变这些原类别标签。

## C01-01｜已存篇幅上限用于新更新说明

**setup（模型初始不可见）：** A：本人明确要求日常更新最多三条、每条一句；B：曾阅读长文的单次episode。

**provider_input（初始可见）：** 把这段改成给我的更新说明，篇幅照我之前定下的习惯：周三上线离线导出，旧格式仍可打开，升级前要备份。

**gold（仅计分端）：** 实际取得A；最多三条覆盖三事实，不执行备份；不能靠简短输出冒充召回。

## C01-02｜明确查询答复语言偏好

**setup（模型初始不可见）：** A：本人要求个人技术答复用简体中文；B：给海外客户的英语特例不适用。

**provider_input（初始可见）：** 我给自己看的技术说明通常要用什么语言？沿用我之前说过的要求。

**gold（仅计分端）：** 取得A并回答简体中文；不引入客户特例，不增加procedure。

## C01-03｜数值展示的小数位规则

**setup（模型初始不可见）：** A：本人要求非整数测量结果保留两位小数；B：财务报表保留整数的项目特例。

**provider_input（初始可见）：** 按我固定的测量结果展示规则，把这次读数3.146写给我。

**gold（仅计分端）：** 取得A输出3.15；不能误用财务范围，不需要历史测量episode。

## C01-04｜用户指定的日期格式

**setup（模型初始不可见）：** A：本人约定笔记日期用YYYY-MM-DD；B：旧活动海报曾用中文日期。

**provider_input（初始可见）：** 用我笔记里一贯的日期格式写出2026年9月6日。

**gold（仅计分端）：** 取得A输出2026-09-06；不拿偶发海报格式当偏好。

## C01-05｜同一用户两类报告的范围区别

**setup（模型初始不可见）：** A：内部进度用三条清单；B：客户报价用表格；均为本人明确语义约定。

**provider_input（初始可见）：** 这份是给内部同事的进度说明。我之前对这类报告的格式是怎么定的？

**gold（仅计分端）：** 选A的内部范围而非B；只回答三条清单，不把两者拼成额外要求。

## C01-06｜显式更新后的称呼偏好

**setup（模型初始不可见）：** A：本人明确把称呼从老师改为小周，当前head为小周；B为superseded旧称呼。

**provider_input（初始可见）：** 现在应怎么称呼我？用我最后明确确认的那个称呼。

**gold（仅计分端）：** 实际获取当前A，只答小周，不把旧称呼列为另一可用选项。

## C01-07｜否定型偏好禁止结尾客套

**setup（模型初始不可见）：** A：本人要求回复不要以“还有什么可以帮你”收尾；B：一般礼貌表达不受禁止。

**provider_input（初始可见）：** 按我对结尾客套话的约定，给“文件整理好了”写一句回复。

**gold（仅计分端）：** 取得A，给简洁完成通知且不加禁用结尾；不是所有礼貌用语都禁用。

## C01-08｜偏好条件例外由当前用途触发

**setup（模型初始不可见）：** A：通常报告要表格，但手机阅读时改短列表；本人明确条件规则。

**provider_input（初始可见）：** 我正用手机看。按我曾定下的阅读格式，把任务甲完成、任务乙待确认整理给我。

**gold（仅计分端）：** 取得A并按手机分支列两条；不强行表格，不执行待确认任务。

## C01-09｜单位习惯不能从偶发经历推断

**setup（模型初始不可见）：** A：本人日常距离用公里；B：一次活动记录写过英里，无偏好变更。

**provider_input（初始可见）：** 我之前要求日常距离显示用什么单位？

**gold（仅计分端）：** 取得A回答公里，排除B单次经历；无需换算或行程回顾。

## C01-10｜列表排序的稳定约定

**setup（模型初始不可见）：** A：本人的待办清单按截止时间升序，没日期的放最后。

**provider_input（初始可见）：** 照我已存的待办排序约定，排一下：提交周五到期、复查无日期、取件周三到期。

**gold（仅计分端）：** 取得A，取件→提交→复查；只是排序，不创建提醒。

## C01-11｜文件名格式作为事实读取不执行

**setup（模型初始不可见）：** A：本人草稿文件命名约定为日期_主题；无步骤程序。

**provider_input（初始可见）：** 我记得定过草稿文件名规则。给今天的“预算”草稿拟一个名字，不创建文件。

**gold（仅计分端）：** 取得A，给2026-09-06_预算；不调用文件工具、不额外查procedure。

## C01-12｜本人工作时段与他人时段区分

**setup（模型初始不可见）：** A：本人工作联系时段09:00–17:00；B：同事夜班20:00–04:00。

**provider_input（初始可见）：** 我平时允许安排工作联系的时段是什么？不要用同事的排班。

**gold（仅计分端）：** 取得A正确绑定本人；不混入B，不据此发送邀约。

## C01-13｜固定术语译名精确读取

**setup（模型初始不可见）：** A：本人词表把checkpoint固定译成检查点；B：另一项目译法为快照。

**provider_input（初始可见）：** 按照我自己的词表，checkpoint应译成什么？

**gold（仅计分端）：** 读取A答检查点；不因通用词典或B改译法。

## C01-14｜明确询问默认时区

**setup（模型初始不可见）：** A：本人日志默认时区Asia/Shanghai；B：服务器位于UTC不代表本人偏好。

**provider_input（初始可见）：** 我以前给个人日志定的默认时区是什么？

**gold（仅计分端）：** 取得A答Asia/Shanghai/北京时间；不读取无关服务器事件。

## C01-15｜长期金额展示币种

**setup（模型初始不可见）：** A：本人比较采购金额统一人民币；B：原始报价可用美元。

**provider_input（初始可见）：** 我以前要求比较采购方案时统一成什么币种？这里只问显示口径。

**gold（仅计分端）：** 取得A答人民币；不联网查汇率或制造换算。

## C01-16｜同一实体两个属性只问其一

**setup（模型初始不可见）：** A：本人摘要应先结论；B：本人会议通常下午；同一用户不同属性。

**provider_input（初始可见）：** 我之前说过摘要应该先写什么？

**gold（仅计分端）：** 取得A答结论；不因同主体顺带召回会议时间。

## C01-17｜偏好存在但结果不应扩大任务

**setup（模型初始不可见）：** A：本人邮件草稿最多两段；B：有一条未到期提醒，与拟稿无关。

**provider_input（初始可见）：** 按我平时的段落要求，替我起草一句“明天交材料”的通知，先别发。

**gold（仅计分端）：** 取得A满足段落上限；不召回无关提醒、不发送。

## C01-18｜偏好适用当前输出渠道

**setup（模型初始不可见）：** A：聊天答复允许项目符号，短信只用纯文本；均为稳定渠道规则。

**provider_input（初始可见）：** 这是要复制到短信里的文字，按我对这个渠道的格式规则，整理“已收到，周四处理”。

**gold（仅计分端）：** 取得A选纯文本分支；不混用聊天项目符号，不实际发短信。

## C01-19｜同义表述定位精确约定

**setup（模型初始不可见）：** A：本人明确规定解释先说结论再说理由；B：一次讨论先聊背景。

**provider_input（初始可见）：** 我之前说过，说明事情是先报结果还是先铺背景？

**gold（仅计分端）：** 用同义理解定位A，回答先结论后理由；不把B当最新偏好。

## C01-20｜比较两项已存事实但仍只需语义类型

**setup（模型初始不可见）：** A：个人日报最多三点；B：个人周报最多五点，两者当前有效。

**provider_input（初始可见）：** 我定下的日报和周报分别最多列几条？

**gold（仅计分端）：** 同一semantic类型取得A/B并正确比较3/5；不是两类型需求。

本文件20条；gold是期望，不是实测结果。零查询、零披露、后台gate是否执行必须分别由真实trace核对。
