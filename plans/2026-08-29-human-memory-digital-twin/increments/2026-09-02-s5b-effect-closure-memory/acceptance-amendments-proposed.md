# Acceptance 修订提案（challenge round-1 产物，待用户批准；批准前 `acceptance.md` 原文不动）

> 依据：plan challenge 5 个 specialist 的实证 finding（`verification/plan-challenge/challenge-synthesis.json`）。
> 批准后由主 agent 合并进 `acceptance.md` 并以 `record-challenge-control --action scope-change-approved --approval-hash <批准原话 sha256> --acceptance acceptance.md` 入账。

| # | 条款 | 现文 | 修订为 | 依据 |
|---|---|---|---|---|
| A1 | 矛盾分析·最小验证动作 / DoD-1 | "…post-turn analysis 产出被记忆库接受的 `MemoryMutationPlan`" | "…post-turn analysis 产出的 `MemoryMutationPlan` 被记忆库接受**并物化**（cognitive head + `memory.cognitive.committed` 或 prospective registration outbox），**下一轮 typed recall 可读到**" | spike A2 4a：0.6.0 accepted ≠ 物化、不可召回（P0 pc-milestone-stops-at-accepted-plan） |
| A2 | S5B-AC-2① | "调 exact Memory 0.6 wheel" | "调 exact Memory **0.6.1** wheel（builder 透传 `supported_filter_policies`；多 operation finalize 收敛；accepted plan 仓储内物化；`register_principal_owner`；analysis lineage 绑在 ingest；base_revision 端口）" | 两个 P0：`host-filter-policy-not-admitted-by-memory-060`、`memory-060-multi-operation-finalize-nonconvergent` |
| A3 | S5B-AC-1③ / D1 | "暂存终答不交付，Host 以同一主模型发起 closure 调用…pending 时终答带 pending 标记交付" | "终答由 SDK delivery pump 按既有路径交付（Host 无法在该 seam 扣押）；Host 在 SDK terminal 之后、Host 终态提交之前完成 closure 兜底（同 Run 绑定主模型、durable attempt、仅当前 lease owner、`handed_off/unknown` 绝不重发）；非法/拒绝/timeout/unknown → durable `semantic_closure_pending`，由 Host 终态 receipt 与 STATUS 投影承载并在**同 scope 的下一 Run** snapshot 注入；scope complete/checkpoint/resume 时仍 pending → Host 强制 `no_mutation(closure_abandoned)` 零 Provider 调用" | `closure-fallback-window-durable-state-and-recovery`、`pending-closure-bound-to-scope-not-conversation`（终答交付点 kernel_terminal.py:160-180） |
| A4 | D2 / S5B-AC-3④ | "…在 standalone 路由或无可信 binding 时被拒绝，模型必须先 create_new/continue/resume" | "standalone 路由下写工具**不可见/不可激活**；模型仍调用则本轮以稳定原因 **fail-closed 整 Run（durable FAILED，零写入）**——冻结 SDK 无模型可见拒绝路径；建立 TaskScope 后**在下一轮**执行写入（Run 的写根在 admission 时冻结，同 Run 再路由到其它 scope 的项目 effect 以 `effect_gate_frozen_scope_mismatch` 拒绝）" | `effect-exposure-hiding-unverified-and-standalone-run-fault`、`effect-envelope-root-vs-frozen-write-scope-split`（spec-effect E1–E3） |
| A5 | S5B-AC-3⑤ | "Auto 只豁免 folder-append 确认；`_CONFIRM_ONLY` 门与 grant 要求不变" | "Auto 豁免 folder-append 确认并**沿用既有可逆本地效果的 auto 策略**（preserve-approved）；`_CONFIRM_ONLY`（发布/删除/付款/凭据/隐私/未知）类在 Auto 下**永不 auto-grant**（`explicit_only` 从冻结 effect 分类计算并接入 SDK 授权路径 `decide()`），有显式断言测试" | `auto-mode-exemption-broader-than-folder-append`（SDK 路径今天全豁免） |
| A6 | S5B-AC-3⑥ / S5B-S4 | "逐 root canary envelope"（TC-HM-09 步骤 6 多 root） | "TC-HM-09 步骤 6 以**每个 canary root 一个单 root scope**执行；两 root scope 的项目 effect 断言稳定 fail-closed（OOS-MULTI-ROOT-SELECTION）；Run 内 Manual append 之后 head ≠ receipt → `workspace_binding_receipt_superseded`（strict）直到下一轮 route 刷新。以 verification-spec oracle 解释 + `behavior_changes` 入账，TC-HM-09 文件不改" | `tc-hm-09-multi-root-canary-vs-oos-and-d2-wording` |
| A7 | S5B-AC-4③⑤ | "模型终答未提及/拒绝 → 不标 processed"；"模型终答提及 → ack" | "**processed := 该 occurrence_key 存在 durable `prospective_ack` receipt**（新 Host 工具 `prospective_ack{occurrence_key}`，五路可见）；模型未调用/拒绝 → 不 processed，下一轮继续 pending；`occurrence_presented` 投影只在 occurrence 离开 mandatory inbox（acknowledged 或 settled superseded/expired）时写入；真实车道的『终答提及』只作 quality_bar 人工检查" | `prospective-ack-channel-undefined`、`prospective-processed-definition-acceptance-wording`、`occurrence-presented-dual-table-state` |
| A8 | S5B-AC-4①② | — | 追加："outbox 消费与游标同一事务；Host 以 outbox_id 派生的确定性 signal 身份回签 `REGISTRATION_ACCEPTED`（crash 重放同 ref）；claims 来源于 inbox；递归意图（『每周五』）在 wheel 只有 time/event trigger 下按**一次性到期**处理，递归留 backlog" | `prospective-registration-accepted-signal-step-missing`、value specialist xref |
| A9 | S5B-AC-6① | 注入清单 | 追加 **Host `evidence_authority`**（只从 Host state.db durable envelope/receipt 解析）与 **`register_principal_owner` 首次构建幂等调用**；缺件 startup fail | `evidence-authority-principal-registration-missing` |
| A10 | S5B-AC-2③ / D7 | "unknown 后经 provider response id 确认成功者返回同一行" | "`sent_unknown` 在非流式 OpenAI 兼容端点无生产确认路径：可执行终态 = 拒绝一切再投递（0 Provider 调用）→ Memory dead_letter + Host durable `memory.analysis.blocked`；`sent_confirmed` 仅经注入的测试 reconciliation observer 可达（S5B-S3 lane 必含）；账本键 = `UNIQUE(request_hash, attempt)` + attempt 无关 `evidence_set_key` + per-member evidence 索引；lease > analysis deadline" | `analysis-idempotency-attempt-bound-unknown` |
| A11 | D5（release unit） | 三高风险子系统计数 | **需用户裁决**：(默认建议) 保持单增量，但验收按三条链分 lane 先后 record-run、Memory 0.6.1 独立 cutover receipt；(备选) AC-4/AC-5 拆 S5c | `release-unit-third-subsystem-packing` |
| A12 | 测试场景矩阵 | — | S5B-S2 seams 追加：observer next_sequence 竞争、attempt 五态 reconcile、跨 Run plan；S5B-S3 追加：≥2 op finalize、audit_pending 卡死、reconciliation observer、lease-reclaim in-flight、membership-growth；S5B-S4 追加：Run fault 三稳定码、frozen-root 分裂、Auto+DESTRUCTIVE；新增 **S5B-S8 真实桌面 UI 通道预演**（1 turn，S1 前置） | `fault-runners-built-after-they-are-needed`、`real-ui-harness-not-a-task` |

用户可感知差异复述（人话）：① 记忆库要出 0.6.1 才能让"这轮聊的事"真正变成可回忆的记忆；② 让 AI 改文件必须先在任务里，闲聊状态下写工具不可见；③ 终答不会被扣住等收口，收口没完成会在任务状态里标"待收口"，下次回到这个任务时补；④ 提醒被"处理"的唯一标准是 AI 明确调用了确认工具，没调用就下次继续提醒；⑤ Auto 模式仍会自动放行可逆的本地改动，但发布/删除/付款/权限类永远要你确认。
| A13 | S5B-AC-1① | "Harness Provider/Tool/Context/Route 证据由 Host 侧 source-ledger outbox 按 `source_event_id+payload_hash` 幂等导入" | "…由 Host 在各事实提交点**同事务直写** `ExecutionEvidenceIngress`（其它 DB 事实先预留 source_sequence，terminal observer 唯一排空），按 `source_event_id+payload_hash` 幂等；不引入异步 outbox worker" | closure round-2 `ac1-outbox-wording-vs-reservation-design`（probe A9 丢行） |
