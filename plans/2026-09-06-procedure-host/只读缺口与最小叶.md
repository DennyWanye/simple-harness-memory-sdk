# Procedure Host：只读缺口与最小实施叶

日期：2026-09-06。Memory 树 `simple-harness-memory-sdk-typed-short-sources`，基线 `ed9996f`。Host 初读 `762af1ab`，交付前重读 `2c8c57c6` 下的提案、builder 和 recall 接线，以下缺口仍存在。公开 Procedure 协议核对既有 H076 installed target；主已推进 H077，本文不声称完成 H077 运行验证。本轮仅新增本文，未改业务源、运行测试、构建或调用 Provider/native。

## 原义务与确定结论

Memory 原 `plans/2026-08-29-human-memory-digital-twin/acceptance.md` 的 HM-AC-5，以及 `slices/S3-cognitive-systems-recall.md:61` 的 Task 3 COMPLETE，限定的是 SDK 规则：明确“以后都这样”可 active，但不授予执行权限；观察型低风险可逆程序需滚动 90 天、相同 revision/applicability、三个独立 TaskScope 成功；一次仅 draft，两次 eligible；高风险观察不自动激活。使用前必须核当前适用性。

Host `testcase/human-memory-program/TC-HM-04-procedure-prospective.md` 步骤 1/2 应分开 fixture：步骤 1 已明确采用，不能再拿它要求步骤 2 降为 draft；步骤 2 应从未明确采用的观察型程序开始。本文不修改原 testcase 或阈值。

下表 Host 路径均相对于 `/Users/denny/projects/simple_harness-primary-candidate`；Memory 路径相对于本文所在仓库。行号供定位，结论以所列函数为准。

| 触发与路径 | 当前实现 | 应有行为与证据边界 |
|---|---|---|
| 实际 USER 证据经 `backend/deskpet/memory/human_memory_service.py:1881` 入场，`analysis_executor.py` 读取 ordered evidence，`analysis_proposal.py:501` 编译 Procedure 提案 | `analysis_proposal.py:383` 一律设 ACTIVE；`:445` 一律 EXPLICIT_USER/SOURCE_BOUND。schema 仅要求 name/steps，没有采用意图判别 | 用户“这次按这些步骤成功一次”是真实用户陈述，但不等于“以后采用”。若 Provider 为前者提出合法 Procedure，当前编译会给 ACTIVE；这是确定的条件调用链，尚未观测真实 Provider 已触发误分类 |
| `analysis_proposal.py:261` derive_span 与 `evidence_authority.py` | 校验实际唯一引文、UTF-8 范围/hash、持久证据身份；span 标为 USER/AUTHENTICATED_USER/EXPLICIT_USER_ASSERTION | 来源真实性不等于采用意图。Memory `core/mutations.py:550` 只阻止非 EXPLICIT_USER 的 Procedure ACTIVE，不能替 Host 修正错误标签 |
| TaskScope/tool 成功进入主对话后，期望资格累计 | `human_memory_v7.py:170` build_kwargs 及 manager 未传 `procedure_observation_authority`；`evidence_authority.py:138` typed observation resolver 明确拒绝未登记观察 | 尚无 Host 生产链将真实独立成功登记为 Procedure observation，不能用普通分析提案或 Run completed 替代 |
| 查询或应用已保存 Procedure | `human_memory_v7.py:385` 构造 RecallContext，当前 Procedure applicability fingerprints 为空；提案缺 applicability 时填 `general` | Memory `backends/sqlite_v5.py:4917` 要求 revision 已绑定的非 UNBOUND 指纹与当前指纹匹配。ACTIVE 存储不等于可召回，更不等于已检查适用性、允许执行 |

不能仅凭 hardcoded USER 标签断言工具内容已冒充用户：当前 `primary_message_v2.py:44` 生成结构化 assistant/tool payload，不含可引用顶层 text；`analysis_proposal.py:237/261` 将其标 nonquotable 并拒绝文本 span。非 USER 负控仍必要，但目前未形成实际绕过证据。

## 既有公开接口可复用到哪里

H076 实际协议文件：`/Users/denny/projects/simple-harness-sdk-short-context-revision/.local-test-evidence/2026-09-06/prospective-rescheduled/artifact-076/installed-target/simple_harness/runtime/procedure_observation_protocol.py`。

| 接口 | 可提供的事实或动作 | Host 尚需提供 |
|---|---|---|
| `ProcedureApplicabilityContext`，上述文件 `:96` | tool_id/environment/tool_version/input_schema_hash 的 v2 指纹 | 实际选用工具、版本、schema 和环境快照；不能以 `general` 或模型自报指纹代替 |
| `ProcedureObservationIntent` `:149`、`ProcedureObservationAuthorityPort` `:489`、`issue_procedure_observation_authority` `:495` | 显式 terminal_outcome/applicability_snapshot，精确目标 revision、TaskScope、evidence span、terminal receipt、outcome/attribution/hazard、run/operation 与 transition | Host 的可恢复、独立核验来源及 ref resolver；不能让模型提交自授 authority |
| Memory `core/manager.py:1306` `record_procedure_observation(*, principal, scope, reference)` | 消费真实 ref，持久 decision/result，独立成功累计和漂移处理 | 实际调用者及上述 authority 注入。SDK 当前能力足够，不需本叶新增 SDK API |
| H076 `execution/audit.py:419` `read_run_operation_audit(run_id, *, limit)` | 有界 Run terminal facts、coverage/truncation；Host `primary_tool_causality.py:24/61` 已沿公开 effect/provider/audit 路径读取工具因果 | terminal 完成事实还不是 Procedure 成功归因或授权；必须检查完整性 |
| Host `primary_message_v2.py:59` tool terminal attestation / conversation registration | 已有实际 terminal、消息 ordinal、工具因果来源和 receipt 绑定 | 将确切程序 revision 的选择/应用记录与同一 TaskScope 工具结果关联，不能另造 receipt |

Memory `backends/sqlite_v5.py:9727` 实际要求：证据 origin 匹配 TaskScope；恰一 conversation registration 的 run 匹配；terminal receipt ID/hash 必须等于该 registration 的 tool causal link。TaskScope head status 或普通 completed Run 不能独立满足这些条件。Host `sdk_adapters/composition.py` 的旧 terminal 私有 SQL reader 也不应成为新 Procedure 事实入口；该文件属 Hegel 范围。

**仍未确认**：真实 Provider 误分类实例；实际程序选择/应用身份与完整注册工具组是否已有可直接复用的持久关联；一个真实 TaskScope 成功如何完整组成上述 intent；H077 对本路径的实际运行结果。本轮没有运行证据，不能称 Host Procedure 完成。

## 推荐最小实施叶：创建时采用意图区分

建议主协调范围后先实施以下 Host 叶；本文没有开始实现。

1. 仅改 `backend/deskpet/memory/analysis_proposal.py` 的 Procedure 创建判别，必要时新增小型 `procedure_intent.py`，以及该判别的局部测试。若需要修改 executor 传递真实证据，应先列出具体参数；不扩大为所有记忆类型重写。
2. 采用意图来自实际入场 USER 证据及精确引文、主体和步骤关联，不能由模型 `explicit=true`、risk_level 或 reason_code 决定。明确采用允许 ACTIVE；仅报告一次成功保留真实 USER epistemic，但创建 DRAFT，资格成功数为零，不将自述伪装为已核工具观察。
3. 不以孤立关键词命中判断采用；否定、引述他人、假设、代码文本和语义不明确均不得自动升格。首叶支持范围须具体可审阅，未覆盖表达保留 draft 和判别原因，不宣称通用自然语言判定正确。
4. 明确采用的高风险程序可以是 ACTIVE 记忆；执行仍需真实工具权限和适用性检查。本叶不签 execution grant，不自动调用工具。
5. 固定新判别版本及原因，沿既有分析持久计划绑定；不重新解释或改写已落盘旧 plan/receipt，不用新规则静默覆盖 replay。实施时核现有 proposal/compiler 版本与缓存键接缝，再确定最小版本变更。
6. 不改 SDK、schema、runtime/composition、Prospective source/store/consumer，不填假 applicability 指纹。当前 observation/application 链仍明确未接完。

最小必要验收只覆盖本次风险，不重复 SDK 已绿资格矩阵：

| 新控 | 要证明的行为 |
|---|---|
| 同一真实 USER 入场链：明确采用 vs 仅一次成功叙述 | 分别 ACTIVE / DRAFT；用户自述不增加 observation 成功次数。至少一条走真实 Host analysis 编译到 installed Memory 公共写入 |
| 否定、他人引述、假设、非 USER、模型自报采用 | 均不能绕过真实采用判别；保留当前非 quotable 防线 |
| 高风险明确采用 vs 观察成功 | 前者仅记忆状态可 ACTIVE，后者不可观察自动激活；两者不凭本叶产生实际 effect |
| 新判别持久计划重开/重放 | 固定决策一致；旧已提交计划不被静默重编 |
| 一条必要适用性边界控 | 未绑定实际 fingerprint 的 ACTIVE 不被本叶变成可用 Procedure，不以 recall 命中代替产品应用验证 |

后续完整接线仍需另协调：真实程序选择/应用事实 → 公开工具因果和 TaskScope 证据 → 持久 observation ref/resolver → Memory 资格结果 → 当前工具环境 snapshot 与使用前检查。Hegel owns runtime/composition，Carver owns Prospective source/store/consumer；本叶不碰这些文件。范围确认是主代理内部协作，不是向用户再申请功能授权。
